---
freeze_status: red
stable_passes: 0
active_node: D
parent_loop: C <-> K
next_edge: D -> C -> E -> I
prose_node: closed
updated: 2026-08-06
---

# Findings freeze

This is the live handoff between the research graph and prose node P. A recent paper commit does not make prose the active node. Node P stays closed until `scripts/audit_findings_freeze.py` passes and two consecutive F to G passes add no claim and retire none.

## Current claim registry

| claim | status | evidence now | blocking attack |
|---|---|---|---|
| The vehicle role moves from the native asset toward stable numéraires | provisional, primary candidate | `data/processed/vehicle_excess_use_daily.parquet`; primary scope is the four prespecified currency types, cycles excluded, direct routes included in endpoint demand. Dated backing regimes show fiat-reserve stablecoins carrying 95.82% and 96.84% of stable intermediary value in 2025 and 2026 against 89.50% and 90.29% of stable endpoint demand; RWA-mixed DAI/USDS falls from a 1.35 within-stable excess-use ratio in 2021 to 0.22 in 2026. The venue rival survives: on constant-product venues, stable excess use rises from 0.65 in 2020 to 1.31 in 2026 while native falls from 1.23 to 0.53. After canonical native-ETH/WETH identity removes 1.19 million false wrapping episodes, 42,974,290 episodes remain. From 2024 to 2026, stable rises 18.6% to 41.2% on single-venue routes and 18.9% to 46.7% on cross-venue routes; native falls 75.7% to 45.7% and 60.7% to 33.7%. On the 181 calendar days shared by both endpoint years, daily HAC estimates within native-plus-stable episodes put the stable-share changes at +24.9 points on single-venue routes (SE 1.8) and +30.5 cross-venue (SE 1.4), both p<0.001. Value weighting is not uniform: the horizon-balanced stable-value change is +20.9 points on single-venue routes (p<0.001) but +1.9 cross-venue (p=0.566), and the all-route change is +3.7 (p=0.164) | report forced-versus-chosen routes, conditional routing-search efficiency, and the 2023 to 2024 native rebound before fixing the shape of the transition; non-USD and small-regime cells remain diagnostics because their shares are below 1% |
| Realised routing persists while its vehicle is cost-dominated | withdrawn pending redesign | the old 27.2%, 41.3%, and 70.1% figures are route-level diagnostics; the old realised join omitted quote hour, while the definition audit requires pair-candidate-period cells. Across 74 evenly spaced V3 dates, strict-before block-log ordering covers 3,636 triangle-days and 1,897,733 direct-pool opportunity snapshots; the hour-boundary verdict flips on 38.63% at zero wedge, 23.53% at 30 bps, 13.36% at 60 bps and 7.42% at 100 bps. The time placebo is monotone in the required direction: the flip rate is 21.75% within one minute of the boundary and 45.22% with 30 to 60 minutes remaining. Actual-route validation passes its narrower contract: across 12,931 realised two-leg V3 routes on three dates, the product of realised leg rates never exceeds the same pools' pre-transaction marginal-rate product; the median fee-and-impact shortfall is 55.7 bps. The legacy exact-size V2-family counterfactual also failed its route-unit and state-order contracts: it grouped raw swaps by whole transaction and selected the last pre-state at or before a timestamp, which omitted the preceding swap's effect and confounded same-second events. On 2022-01-15 this admitted 743 supposed routes with a +6,788 bps p99 and +514,421 bps maximum. Canonical route components and strict block-log state first reduced the sample to 229, after which adjacent-hour swap-only continuity over-pruned it to 132. Uniswap v2 mint/burn replay explains 95.2% of flagged pool-hours on that date, and 97.4%, 90.6%, 88.8%, 73.8% and 49.7% on the five other fixed validation dates from 2021 to 2025. Replaying those events and advancing the nearest prior observed reserve through every intervening raw change yields 258 traceable routes. On the notional denominator, 13.2% have a better direct route gross of gas; the median advantage is -87.0 bps, p90 is +21.5 bps, p99 is +311.0 bps and the maximum is +817.6 bps. The strict adjacent-state/no-liquidity slice is 11.2% on 134 routes, the bridged-state slice 10.6% on 104, and the liquidity-replay slice 19.0% on 42, so the last requires an explicit robustness split. Twenty-seven of the 34 gross-dominated routes have their best direct pool outside the realised venue set. The canonical full-block daily gas panel and pooled one-hop/two-hop topology medians raise direct dominance to 53.5% on all 258 routes; that adjustment is diagnostic until gas is venue- and router-specific. These are repair statistics, not a finding. The corrected instrument writes a provenance-stamped panel and real JSONL summary, and every row retains its three prior-state gaps and liquidity-event counts. Restricted venue coverage and omitted gas both favour the realised two-hop route, so gross direct-dominance incidence is a lower bound | rerun the corrected exact-size V2-family branch on the fixed monthly design, report adjacent/no-liquidity and replayed-support splits, classify venue reach versus within-set search, replace topology-median gas with venue/router-specific receipts, then estimate continuous signed gaps at the pair-candidate-period level; retain the opportunity-time triangle only as a timing bound |
| Losing the cost advantage barely changes vehicle turnover | preliminary, cannot lead | the unconditional one-venue block sample reports a 1.17 hazard ratio | estimate conditionally on the full candidate/pair/day panel, with pair and time absorption and candidate-count controls; reject the claim if it does not survive |
| Native-long-tail vehicle liquidity is supplied at a loss | survives as a headline candidate | `docs/finding-rent-incidence.md`; two independent venue families agree and the LVR attribution is corrected | finish gas incidence by venue and candidate; separate backing regimes; state the CEX-price support bound |
| The transition is fragmentation without succession | retired | betweenness is nearly degree and HHI plus leader cannot distinguish split cells from separate monopolies | rebuild on per-cell regimes and switching order; direction is open |
| Cross-venue spillovers identify the mechanism | retired | the untreated restriction recovers most of the all-venue estimate and Merge placebos fire | no control group is identified; retain only bounded descriptive facts |

## Data and execution gates

| gate | live state |
|---|---|
| Route-cost panel | the 123,262,704-row build is pre-correction and cannot support a frozen finding. It contains 1,372,248 duplicated quote cells because canonical ETH/WETH endpoint addresses were regrouped by non-canonical display symbols; every duplicate has multiplicity two. The selector now aggregates on canonical addresses, rejects components with ambiguous canonical endpoints and assigns labels after aggregation. Strict atomic all-shard assembly is implemented, but every day must be repriced under the new cache key after the v4 contract passes |
| Uniswap v4 | exact-ID enrichment is complete on all 523 raw calendar files, with zero incomplete or invalid statics across 29,005,863 swaps. Vanilla static-fee quote support covers 84.02% of swaps and 99.05% of reported value pooled; 2026 coverage is 79.89% and 95.57%. The excluded rows are hook-bearing or dynamic-fee-plus-hook pools. Strict early/mid/late validation re-quotes 4,218 realised swaps: 100% fall within 1% in both directions and tick-crossing cells, median absolute error is 0.0000%, p99 is 0.00000039% and the maximum is 0.000998% |
| Vehicle extent | full-sample candidate-currency panel built over 2,277 days and 8,219,702 token-days; backing ratios are now normalized within stablecoins after the first rebuild incorrectly left non-stable candidates in the denominator. All-asset version is retained only as a diagnostic because residual contracts carry 19 to 22 percent of 2022 to 2023 intermediation |
| Routing efficiency / aggregator era | opportunity-set integration is measured on 358,027,668 clean route units with canonical endpoint identity: cross-venue economic multi-leg routing rises from 1.4% to 58.7% by count and 15.4% to 89.3% by value. The annual economic multi-leg ratio among economic routes falls from 19.5% in 2022 to 16.6% in 2026; on the 181 calendar days shared by both endpoint years, the equal-weighted daily change is -1.61 percentage points (Newey-West SE 0.33, p<0.001). A balanced five-venue perimeter shows cross-venue count still rising from 19.1% to 43.6% and value from 49.4% to 55.5%, while the annual indirect ratio nearly halves from 19.5% to 10.3% (horizon-balanced daily change -8.02 points, SE 0.25, p<0.001); routes above two legs rise from 15.1% to 20.0%, mean legs from 2.25 to 2.39 and mean venues from 1.22 to 1.47. The large balanced decline fails the support-exit gate: route coverage falls from 100.0% to 69.8%, and entrant-touching routes have 31.3% indirect incidence in 2026. It is a decomposition showing that incumbent-only routing becomes more direct while complex routes migrate to entrants, not an efficiency estimate. The earlier raw-address filter misclassified 310,595 routes in 2025 and 321,473 in 2026 whose endpoints were native ETH and WETH; canonicalisation revises the 2026 cross-venue count share from 60.6% to 58.7%. This documents integration, market-wide incidence contraction and route complexity, not optimisation quality or aggregator causality. Across 42,974,290 canonical intermediary episodes, the 2024 to 2026 native-to-stable count transition occurs inside both single- and cross-venue strata; on the same 181-day endpoint support, stable-share changes are +24.9 points on single-venue routes (SE 1.8) and +30.5 cross-venue (SE 1.4), both p<0.001, so migration into integrated routes cannot explain it alone. Horizon-balanced value migration is concentrated in single-venue routing (+20.9 points, p<0.001) and absent cross-venue (+1.9, p=0.566). The same-hour forced/chosen matcher, within-vehicle frontier and alternative-path frontier are implemented. An audit found that the preliminary grid matcher could blend different winning paths at the lower and upper quote sizes, constructing a frontier that was never executable; venue-only identity also missed switches between pools on the same venue. It now retains exact pool identities and quarantines any intermediary, venue-sequence or pool-sequence switch. Same-path interpolation remains provisional pending exact-size validation. On sampled 2020, 2022 and 2024 days, the pre-correction same-vehicle diagnostic retained 16/772, 565/6,331 and 288/18,065 routes, with median shortfalls of -0.80%, +0.02% and -0.14%; the pre-correction direct-or-alternative-vehicle frontier retained 16, 1,242 and 381, with medians of -0.80%, -0.06% and -0.08%. These stale counts are retained only to identify the superseded diagnostic and cannot support a claim. Negative values also show that end-of-hour state movement remains mixed into the comparison. A strict transaction-order validator covers 12,931 actual two-leg V3 routes on 2022-06-15, 2024-06-15 and 2025-09-14. Own-state rates pass with zero negative shortfalls and a 55.7 bps pooled median, while 62.8% of routes move by more than 30 bps before the hour closes and 36.4% move by more than 100 bps. Median absolute displacement falls across the three dates from 140.3 to 52.1 to 31.7 bps, a diagnostic consistent with faster price convergence but not a trend estimate. The full triangle-gap maturation run separates recurrent from horizon-balanced support. Recurrent economic triangles compress by 15.9% to 16.5% a year across recurrence thresholds (p<0.001), but the horizon-balanced economic set carries 18 identities over 397 triangle-days and estimates only 6.4% (p=0.126), falling to 5.5% (p=0.177) on the 15 identities observed at least ten dates. The significant exact-pool horizon row is inadmissibly concentrated in three pool triangles, two at the ten-date threshold. The balanced economic result is therefore a null on market-wide price-gap compression; the recurrent result remains a lifecycle/composition diagnostic. The full corrected realised-route match and transaction-state counterfactual remain open |
| Panel-dependent refresher | restricted to validated support, screened-window, and arbitrage-bound diagnostics; all finding estimators remain withheld until their definitions lock |
| Empirical inference | exact alternating-projection absorption replaces the one-pass two-way demeaning formula that was invalid on unbalanced panels. The shared cluster-robust estimator now also computes the exact absorbed fixed-effect rank and includes it in the CR1 finite-sample correction; all affected exhibits must be regenerated. On the sparse 57,989-row challenger panel, the corrected coefficients remain strong: challenger response 4.825 (SE 0.366, t 13.17), incumbent response -3.872 (SE 0.420, t -9.22), and challenger-overtakes-incumbent 9.947 (SE 0.486, t 20.48). The shared estimator now optionally applies a Bartlett HAC correction across ordered date-cluster scores, preserving within-date cross-sectional dependence while allowing serial dependence between sampled dates. The corrected triangle maturation run absorbs exact identity rank, uses a three-date-cluster lag, and separates recurrent from horizon-balanced support; the balanced economic estimate is 6.4% annual compression (SE 0.0425, p=0.126) on 18 identities and 397 triangle-days |
| Historical gas | the canonical daily panel now covers all 2,277 project days with zero unresolved dates. It measures every transaction in three evenly spaced full blocks selected from the active V1, V2 or V3 swap calendar; 2,258 days resolve all three blocks and 19 resolve the prespecified two-block minimum. Median support is 590 transactions per day (range 14 to 2,614). Annual median gas price is 45.00, 84.00, 21.85, 22.00, 9.00, 1.00 and 0.13 gwei from 2020 through 2026. The old twelve-row combined gas/ETH artefact sampled only three receipts per selected day and has no code consumer, so it remains retired. Gas units are a separate route-topology/venue receipt estimand, and daily ETH price comes from the canonical on-chain token-price owner |
| Prose | frozen; existing paper and deck are evidence maps, not final deliverables |

## Loop position

Active execution node: `D`, opened by the `C <-> K` definitions-and-ideation loop. On success the next edge is `D -> C -> E -> I`: validate the corrected data layer against the definitions, lock the efficiency specifications, then run cross-family review. The replacement vehicle-extent definition, dated backing attack, venue-technology rival, single-versus-cross-venue split, balanced venue perimeter and historical v4 contract are implemented. The actual-route transaction-order validator passes its causal-order and amount-orientation contract, after rejecting a zero-decimals default and separating per-leg effective rates from a tolerated intermediate-flow mismatch. The within-vehicle and alternative-path diagnostics remain unfrozen because their counterfactual routes are still priced at hour end. The corrected route-cost panel and triplet-period dominance design remain red. Node P is closed. Any defect found here routes back to C/E; no defect is patched only in prose.

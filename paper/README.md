# Paper Workspace

This folder will contain the manuscript source, supplement, bibliography, and paper-specific build notes.

Generated tables and figures should be read from `../output/tables/` and `../output/figures/`. Data should never be read directly by LaTeX.


% The route-cost benchmark is defined here; no route-cost estimate enters the paper.

\section{Comparing direct and intermediated execution}
\label{sec:executability}

Realised routes identify the assets that carried each trade. Execution cost is a separate object: it compares those routes with alternatives available immediately beforehand, quoted at the same trade size and over a common set of exchanges. We hold the source $i$, destination $o$, dollar input $q$, and pre-transaction pool state $e^-$ fixed. The best direct and two-leg outputs through intermediary $k$ are
\begin{equation}
\label{eq:direct}
O^{D}_{i,o,q,e}=P_{o,e}\max_{p\in\mathcal P_{i,o,e^-}}Q_p\!\left(i\to o,\Delta_i(q,e);\chi_{p,e^-}\right),
\end{equation}
\begin{equation}
\label{eq:vehicle}
O^{I}_{i,o,k,q,e}=P_{o,e}\max_{p'\in\mathcal P_{k,o,e^-}}Q_{p'}\!\left(k\to o,\max_{p\in\mathcal P_{i,k,e^-}}Q_p\!\left(i\to k,\Delta_i(q,e);\chi_{p,e^-}\right);\chi_{p',e^-}\right)
\end{equation}
This comparison extends the realised-versus-optimised allocation benchmark of \citet{XiMoallemi2026Routing} from parallel pools for one atomic asset pair to sequential routes through an intermediary.

The vehicle-route shortfall relative to direct execution is
\begin{equation}
\label{eq:gap}
\Delta C^{D}_{i,o,k,q,e}=\frac{O^{D}_{i,o,q,e}-O^{I}_{i,o,k,q,e}}{O^{D}_{i,o,q,e}},
\end{equation}
reported in basis points as $10^{4}\Delta C^{D}_{i,o,k,q,e}$. Positive values mean that the direct route returns more output before gas. Swaps and liquidity changes are replayed in on-chain order because a daily or hourly closing state incorporates events that occurred after the trade. The observed event marks settlement; a quote formed earlier can reflect a different opportunity set.

We compare hypothetical legs only within a common executable range. Let $\widetilde Q_{\ell}$ denote leg $\ell$'s output at the pool's marginal rate. Each leg satisfies
\begin{equation}
\label{eq:support}
\pi_{\ell}(\Delta_i,e^-)=\frac{\widetilde Q_{\ell}(\Delta_i,e^-)-Q_{\ell}(\Delta_i,e^-)}{\widetilde Q_{\ell}(\Delta_i,e^-)}\leq \bar\pi,
\qquad \bar\pi=0.05.
\end{equation}
The 5\% own-price-impact ceiling applies across pool families; contract-specific limits add further restrictions. Appendix~\ref{app:quoters} gives the pricing equations and Appendix~\ref{app:support} calibrates the support rule.
% output/exhibits/quoter_support_bounds.jsonl; common 5 percent target support rule

Receipt gas provides a narrower execution-friction check. Rather than comparing pool outputs, it measures the fixed toll attached to adding a vehicle leg. That toll matters because gas fees can change both trading costs and liquidity concentration in decentralised exchange \citep{CaparrosChaudharyKlein2024BlockchainScaling}. In the executed-route gas sample, a stable-vehicle route in 2024 uses \GasStableExtraGasUnitsBase{} more gas than the median direct route, \GasStableExtraGasPctBase{} of the direct-route median. Evaluated at that year's median gas price and WETH price, the extra toll is \GasStableExtraCostBase{}, so a trade needs about \GasStableOneBpNotionalBase{} of notional before the extra gas is below one basis point. In 2026 the median stable-vehicle route uses \GasStableExtraGasUnitsEnd{} more gas than the direct-route median, but the dollar toll falls to \GasStableExtraCostEnd{} and the one-basis-point notional falls to \GasStableOneBpNotionalEnd{}. Pricing the same fixed toll at each stable-vehicle route's own gas price sharpens the interpretation: median stable-vehicle notional falls from \GasStableRouteMedianNotionalBase{} to \GasStableRouteMedianNotionalEnd{}, while the median fixed toll falls from \GasStableRouteMedianTollBpsBase{} to \GasStableRouteMedianTollBpsEnd{} and the share of stable-vehicle routes with a fixed toll below 25 basis points rises from \GasStableRouteTwentyFiveBpShareBase{} to \GasStableRouteTwentyFiveBpShareEnd{}, the trade-size version of the fixed-cost channel in \citet{CaparrosChaudharyKlein2024BlockchainScaling}. Thus the extra-hop overhead remains visible in gas units while becoming economically small for ordinary routed notionals. This evidence supports the interpretation that vehicle formation moved into markets where fixed execution overhead had ceased to be a large barrier; it is not an all-in proof that the vehicle route beat the direct alternative on price impact and fees.
% output/exhibits/route_gas_economics.jsonl and
% output/exhibits/route_gas_economics_support.jsonl; macros from
% scripts/tabulate/build_route_gas_economics_deck_values.py. Boundary:
% executed-route receipt gas only, not the same-state route-cost comparison.

The benchmark represents public on-chain liquidity in the declared exchange set. Private order flow, financing costs, losses from reverted transactions, and pools outside that set can alter a router's choice. Pool-family coverage is economically material. In the sampled Curve exercise, pools outside the StableSwap model carry 34.2\% of assessed volume, including 65.2\% of native-leg volume and 21.1\% of stablecoin-leg volume. Hook-bearing or dynamic-fee Uniswap v4 pools, SushiSwap v3, and several Balancer families also use pricing states outside the benchmark. Appendix~\ref{app:coverage} quantifies these boundaries.
% docs/findings/venue-coverage.md; output/exhibits/curve_excluded_composition.jsonl

% Section 2. Institutional setting, definitions, and data.
%
% Every number here is read from an exhibit under output/exhibits/ or from a
% docs/findings/* file, and the nearby comment names the source.

\section{Setting and data}
\label{sec:setting}

\subsection{Routes and notation}
\label{sec:setting:route}

Decentralised exchanges distribute liquidity across smart-contract pools that routers can combine atomically. A trader may use one pool, divide an order across several pools, or trade through an intermediary asset. Because the pool calls share a transaction, the intermediary sequence is observable.\footnote{Ethereum records a transaction's ordered logs in its receipt; see the official \href{https://ethereum.org/developers/docs/apis/json-rpc/}{Ethereum JSON-RPC documentation}.} A route through an intermediary is unavoidable when no direct pool connects the assets; when direct and indirect pools coexist, the router compares their fees, price impact, gas, and venue access. \citet{LeharParlour2024Uniswap} describe the constant-product design and the economics of liquidity supply in these markets.

Blockchain logs record each pool call in execution order. We recover a route by following the directed token flows within a transaction. Calls with a common source and destination form a split order; a token received in one call and spent in a later call joins the calls into a sequential route. The same rule separates unrelated conversions bundled in one transaction. For a reconstructed sequence $A\rightarrow B\rightarrow C$, $(A,C)$ is the pair, $A\rightarrow B$ and $B\rightarrow C$ are its legs, and the full sequence is the route. All three objects are directed by token flow. Each leg executes in a liquidity pool; a pair need not have a direct pool. The observed route endpoints belong to the connected pool component and need not coincide with the endpoints of a broader user instruction.

A transaction on January 10, 2026 illustrates both the information in the logs and its limit. A public explorer describes a 460,000 PYUSD--USDe swap executed through 0x.\footnote{Transaction \ethtx{0xbda1d07f17c503b5a9c8117c5d1383472006af8a9e22aa4e5e30804b9bf67ad9}{0xbda1d07f\ldots 9bf67ad9}.} The user's instruction begins with PYUSD, but the connected pool sequence begins when 459,929.81 USDC enters Fluid. Fluid returns 460,331.05 USDT, which then enters Uniswap v4 and produces 460,104.28 USDe. The observed pair is therefore USDC $\rightarrow$ USDe; its legs are USDC $\rightarrow$ USDT and USDT $\rightarrow$ USDe, and USDT is the vehicle. The earlier PYUSD $\rightarrow$ USDC conversion occurs outside the connected pool sequence. The data do not show whether USDC was another vehicle or inventory supplied by the executor. We apply the connected-component rule to every transaction and do not infer an unobserved leg from nearby transfers. This choice may understate the intermediary role of assets such as USDC when a broader instruction is funded outside the observed pool sequence.
% output/exhibits/route_replay.json; data/unified/20260110.parquet; Blockscout transaction page retrieved 2026-08-14

The sequence directly identifies intermediary use. Conditional on reconstructed pool states immediately before execution, it also defines a same-size cost comparison between the observed route and feasible alternative paths. We introduce notation for the route here and develop the cost comparison in Section~\ref{sec:executability}.

For a route $r$, let $e(r)$ record its block, transaction position, and first pool-event position; $e(r)^-$ is the state immediately before that transaction begins. A route carries an ordered pool sequence $(p_{1},\ldots,p_{m(r)})$ and token sequence $(k_0,\ldots,k_{m(r)})$, where $k_0=i$ is the input and $k_{m(r)}=o$ is the output. The dollar input notional $q$ corresponds to $\Delta_i(q,e)=q/P_{i,e}$ units of the input token. Let $\chi_{p,e^-}$ denote pool $p$'s pretrade state and let $Q_p(i\to j,\Delta_i;\chi_{p,e^-})$ denote its exact-input output in token $j$. Composing the pool quotes gives the route's token output and dollar value,
\begin{equation}
\label{eq:route}
\begin{aligned}
\Delta_{k_h}&=Q_{p_h}\!\left(k_{h-1}\to k_h,\Delta_{k_{h-1}};\chi_{p_h,e(r)^-}\right),
&&h=1,\ldots,m(r),\\
Q_r(q,e(r))&=\Delta_o,
&O_r(q,e(r))&=P_{o,e(r)}Q_r(q,e(r)).
\end{aligned}
\end{equation}
The venue-specific forms of $Q_p$ appear in Appendix~\ref{app:quoters}. Pool invariants take token quantities as arguments, which is why the dollar notional $q$ and the token input $\Delta_i$ remain distinct. Equation~\eqref{eq:route} describes an executed route and, once market state has been reconstructed, prices a counterfactual path.

Intermediary use is one of several currency roles. Invoicing concerns the currency named in trade contracts \citep{GopinathEtAl2020DCP,AmitiItskhokiKonings2022Dominant,Mukhin2022InternationalPriceSystem}; funding concerns the denomination of liabilities \citep{GopinathStein2021Making,ErenMalamud2022DominantDebt}; and dealer turnover records trading activity \citep{Somogyi2026DollarDominanceFX}. These roles can reinforce one another. The route in Equation~\eqref{eq:route} identifies the asset used between the currency sold and the currency bought.

This route setting makes the classical volume--cost mechanism observable at transaction level. Greater use of a vehicle expands trading in the pools supporting its two legs; deeper pools can then lower the cost of using the same vehicle again \citep{Krugman1980VehicleCurrencies}. In \citet{KiyotakiWright1989MoneyMedium}, traders accept an asset for indirect exchange when its marketability compensates for its storage cost. \citet{DowdGreenaway1993CurrencyCompetition} emphasise the fixed costs of learning, accounting, and quoting in a new currency. Liquidity placement, venue access, and the routes available to software provide corresponding coordination forces on decentralised exchanges.

\subsection{Measuring vehicle dominance}
\label{sec:setting:definitions}

An exact two-leg route consists of one pool trade on either side of a unique intermediary and identifies one mutually exclusive vehicle choice for each endpoint exchange. A longer route may contain both a native asset and a stablecoin. We retain those routes in two complementary measures: each intermediary position contributes to the full-route share, and route participation records whether a currency appears anywhere inside a route. Section~\ref{sec:rivals:complexity} shows that stablecoins gain in both the two-leg choice sample and routes with more than two legs. For route $r$, let $\mathcal{M}(r)$ denote the set of assets between its source and destination. We record the intermediary status of asset $k$ as
\begin{equation*}
Z_{r,k} \;=\; \mathbf{1}_{\{k \in \mathcal{M}(r)\}}.
\end{equation*}
Let $a(k)$ assign each token to an economic asset type. The native group consolidates ETH and its one-for-one wrapped representation, WETH, after route reconstruction. The stable group contains fiat-reserve, on-chain-collateralised, synthetic, and non-dollar stable currencies. For $g\in\{\mathrm{native},\mathrm{stable}\}$, define $Z_{r,g}=\sum_{k:a(k)=g}Z_{r,k}$; on an exact two-leg route, this sum is an indicator.

This grouping preserves a token-level estimand. WETH is the one-for-one token representation that allows ETH to interact with ERC-20 pool contracts; wrapping changes the technical form, not the underlying settlement exposure. We therefore reconstruct routes at the token level and consolidate ETH and WETH only when assigning the economic asset used as a vehicle. Liquid-staking tokens remain separate because they also convey staking returns. Dollar-pegged tokens remain distinct instruments because their issuers, backing, redemption arrangements, risks, and leg-level liquidity differ \citep{GortonZhang2023Wildcat,CataliniDeGortariShah2022Stablecoins,LyonsViswanathNatraj2023Stablecoins}. USDT in the USDC--USDT--USDe example is therefore an intermediary token even though all three instruments target the same unit of account. The aggregate stable group measures dominance by a family of stable tokens, while the token-level results retain issuer-specific competition.

Native assets and stablecoins define the main comparison because they are the two persistent broad vehicle families in Figure~\ref{fig:vehicle-composition}. Staked-native, imported, and other intermediaries remain in the wider time series and supplementary results; they do not enter the native-versus-stable denominator. For routes with more than one intermediary, realised intermediary counts retain each asset used along the route. Appendix Table~\ref{tab:network-position} also compares realised use with graph betweenness. Betweenness records how often an asset could lie on paths in the observed trading graph; route shares record the intermediary currencies traders actually use.

Let $\mathcal R^{(2),NS}_t$ denote exact two-leg routes on day $t$ whose intermediary belongs to either the native or stable group, and let $\mathcal R^{(2),NS,V}_t$ retain routes for which the source, intermediary, and destination values agree within 20\%. The equally weighted and dollar-weighted group shares are
% src/ddvc/route_roles.py, component_value_support
\begin{equation}
\label{eq:dominance}
S^{N}_{g,t} \;=\;
\frac{\sum_{r \in \mathcal{R}^{(2),NS}_{t}} Z_{r,g}}
     {|\mathcal{R}^{(2),NS}_{t}|},
\qquad
S^{V}_{g,t} \;=\;
\frac{\sum_{r \in \mathcal{R}^{(2),NS,V}_{t}} \nu_{r} Z_{r,g}}
     {\sum_{r \in \mathcal{R}^{(2),NS,V}_{t}} \nu_{r}},
\end{equation}
where $\nu_r$ is the arithmetic mean of the route's total dollar value at its observed source and destination. The 20\% rule also requires every intermediary's incoming and outgoing dollar values to have a ratio between 0.8 and 1.2. Thus $S^N_{\mathrm{stable},t}$ is stablecoins' aggregate share among native and stable intermediaries, and $S^V_{\mathrm{stable},t}$ is the corresponding dollar-weighted share. The two groups exhaust this denominator. Share levels are reported in percent; the unit for absolute share changes is one \ac{pp}.
% src/ddvc/route_roles.py, component_value_support and component_notional

Appendix~\ref{app:additional-composition} reports two complementary comparisons: all-route participation and intermediary use relative to endpoint demand. It also examines the two largest reserve-backed stablecoins separately. These measures retain longer routes and the broader set of native, staked-native, stable, and imported currencies; Equation~\eqref{eq:dominance} remains the measure used for the aggregate native-versus-stable rotation.

\subsection{The route panel}
\label{sec:setting:data}

The principal route panel covers Curve, Uniswap v1 through v4, Balancer, SushiSwap v2 and v3, and Fluid on Ethereum. We selected these exchange families for their historical Ethereum volume and retained the deployments whose transaction-level pool events and token identities can be reconstructed consistently. The panel spans \RoutePanelCalendarDates{} dates from \RoutePanelSpan{} and covers major constant-product, concentrated-liquidity, stable-swap, weighted-pool, singleton, and integrated-liquidity designs. Other Ethereum exchanges remain outside it. Appendix Table~\ref{tab:app:venues} reports both volume shares within the retained source series and the selected families' share of total Ethereum DEX volume in DeFiLlama.

Indexed pool records or factory registries usually link a pool to its token contracts. Uniswap v1 records the exchange contract in each transaction, and its immutable exchange registry maps every observed contract to a token address and symbol. Shared transaction hashes and matched ETH amounts then identify the ordered legs of v1 token-to-token routes. Section~\ref{sec:rivals:architecture} uses these records to study the protocol rule that made ETH intermediation compulsory.
% output/exhibits/unified_route_quality.jsonl

The source data contain \RoutePanelRawSwapsExact{} pool-level swap records. Indexed blockchain data supply events for eight exchange series, Dune's \texttt{dex.trades} table supplies Fluid records, and direct Ethereum queries supply block headers, ordered logs and receipts, token precision, factory registries, and state checks.\footnote{See \href{https://thegraph.com/docs/en/subgraphs/overview/}{The Graph's indexing documentation}, \href{https://docs.dune.com/data-catalog/curated/overview}{Dune's curated-data documentation}, and the official \href{https://ethereum.org/developers/docs/apis/json-rpc/}{Ethereum node-interface documentation}.} After identifying trade direction and pool events and standardising token units, \RoutePanelUsableLegsExact{} directed pool-swap legs remain. We order the legs within each transaction and apply the flow rule above to distinguish single-pool trades, split orders, sequential routes, and unrelated calls. Appendix~\ref{app:route-construction} gives the reconstruction algorithm and reports the historical token units that we cannot verify.
% output/exhibits/unified_route_quality.jsonl

Transactions with more than one disconnected pool-event group pose a separate measurement limit: the observed calls do not reveal whether the groups belong to one broader instruction \citep{XiMoallemi2026Routing}. The principal sample excludes every component in those transactions. Appendix Table~\ref{tab:app:disconnected} treats each internally valid component as a separate route and finds a slightly larger stablecoin rotation.
% output/exhibits/disconnected_component_boundary.jsonl; output/tables/disconnected_component_boundary.tex

Every non-self-returning token sequence retained by the connected-transaction rule enters the count measure. Dollar-weighted results require source, intermediary, and destination values to agree within 20\%. This is an eligibility test for a coherent dollar notional, not a control for route characteristics. Keeping the count sample broader preserves observed intermediary choice when valuation is noisy; Appendix~\ref{app:roundtrip} reports the separate round-trip exclusion, and each value exhibit reports the stricter sample's coverage.
% output/exhibits/unified_route_quality.jsonl; README.md section 2

Transactions and contract-state changes are publicly observable \citep{Schar2021DeFi}. Reconstructing a route still requires matching pool events, venues, and token units. This procedure identifies the executed route and its calling contract, while the executor address does not establish which frontend or software formed the quote \citep{XiMoallemi2026Routing}. The results are therefore attributed to executed routes, not to a particular routing service.
% docs/research/router-identification.md

Table~\ref{tab:panel} reports the resulting panel coverage and route frequencies. Appendix~\ref{app:route-construction} gives the route-construction checks, while Appendix~\ref{app:quoters} validates the pretrade pricing models used for the counterfactual comparisons.

\begin{table}[htbp]
\centering
\small
\caption{The route panel}
\label{tab:panel}
\begin{tabular}{lr}
\toprule
 & Route panel \\
\midrule
Dates in the full route panel & \RoutePanelCalendarDates{} \\
% output/exhibits/unified_route_quality.jsonl
Missing source-days & \RoutePanelMissingSourceDays{} \\
% output/exhibits/unified_route_quality.jsonl
Raw swap rows & \RoutePanelRawSwapsExact{} \\
% output/exhibits/unified_route_quality.jsonl
Usable directed route legs & \RoutePanelUsableLegsExact{} \\
% output/exhibits/unified_route_quality.jsonl
Routes per day, median across eligible days & 153,586 \\
% output/exhibits/round_trip_share_by_day.jsonl, 2,449 eligible days
Multi-leg routes per day, median across eligible days & 30,064 \\
% output/exhibits/round_trip_share_by_day.jsonl, 2,449 eligible days
Multi-leg share of routes, median across eligible days & 19.4\% \\
% output/exhibits/round_trip_share_by_day.jsonl, 2,449 eligible days
Round trips, median share of multi-leg routes by count & 12.8\% \\
% output/exhibits/round_trip_share_by_day.jsonl, 2,449 eligible days
Round trips, median share of multi-leg routes by value & 21.0\% \\
% output/exhibits/round_trip_share_by_day.jsonl, 2,449 eligible days
\bottomrule
\end{tabular}
\exhibitnote{The full \RoutePanelDeploymentCount-deployment panel contains UTC dates from \RoutePanelSpan{}. Uniswap v1 enters these totals through the exact exchange-to-token registry; its protocol-architecture comparison is venue-specific. Daily route statistics and round-trip shares use all 2,449 eligible dates from 15 May 2019 through 30 June 2026. A multi-leg route traverses more than one pool; a round trip ends in the token with which it began. Round-trip shares are formed among multi-leg routes.}
\end{table}

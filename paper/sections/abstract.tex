% Working-paper abstract: update whenever a substantive result changes.
Transaction-level decentralised-exchange routes reveal which asset carries each indirect trade. Stablecoins' route share rises from \StableCountBase{} to \StableCountEnd{} between early 2024 and 2026. An exact decomposition assigns \PairPooledWithin{} of the \PairPooledTotal{} route-count change and \PairValueWithin{} of the \PairValueTotal{} dollar-weighted change to net substitution within continuing ultimate pairs. \MarginWithinGainPairs{} pairs shift toward stablecoins and \MarginWithinLossPairs{} toward native assets, so gross movements offset. Reweighting and year-specific ultimate pairs carry the aggregate increase. When persistent stable support appears, relative weak-leg depth predicts route share; parity with WETH depth brings stable share to \BridgeDepthEqualFirstShare{}. Deposited depth is a proximate liquidity-formation margin.

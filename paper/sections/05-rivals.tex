\section{What can account for the rotation?}
\label{sec:rivals}

The allocation results sharpen the mechanism question. Stablecoin dominance rose chiefly as routed activity moved across source--destination endpoint pairs and the incidence of intermediation changed, while net replacement of a native intermediary within continuing endpoint pairs contributed little. Any explanation for the rotation must therefore account for the assets being exchanged as well as the intermediary carrying the route.

\subsection{Path complexity and exchange span}
\label{sec:rivals:complexity}

A competing account assigns the rotation to how routes are assembled. Paths grew longer and reached across more exchanges over the sample, and long paths already favoured stablecoins: in 2024 stablecoins carried \MultiLegCountBase{} of native-plus-stable intermediary episodes on routes of more than two legs, against \StableCountBase{} on two-leg routes. That account predicts a roughly flat stablecoin share inside each complexity class and an aggregate increase produced by activity moving toward longer routes that were already stablecoin-intensive.

Stablecoin share rises within every complexity class. Among intermediary episodes on two-leg routes, the stablecoin share rises \StableCountChange{} by count, with a standard error of \StableCountSE{}, and \StableValueChange{} by value (\StableValueSE{}). Among episodes on routes of more than two legs it rises \MultiLegCountChange{} (\MultiLegCountSE{}) and \MultiLegValueChange{} (\MultiLegValueSE{}), from a base of \MultiLegCountBase{} to \MultiLegCountEnd{} by count. Both classes move, and the class with the shortest paths moves further, which leaves reweighting toward complex routes too small to supply the aggregate change. Dividing each complexity class again by whether the route touched one exchange or several leaves four groups, and every one of them rises; the weakest is \WeakestComplexityCountChange{} by count (\WeakestComplexityCountSE{}) and \WeakestComplexityValueChange{} by value (\WeakestComplexityValueSE{}). The stablecoin gain is thus common to short and long paths and to routes that stay on one exchange and cross-exchange routes.

Two boundaries limit what this comparison licenses. The number of legs records the path a trade actually took, so it measures construction; extra legs have no ordinal interpretation for execution quality. Complexity and exchange span are also jointly selected outcomes, since the same conditions that make a longer or wider path attractive can bear on the choice of intermediary. The comparison locates the stablecoin increase within each route-complexity and exchange-span class. The effects of routing software and exchange span remain unidentified.
% output/exhibits/intermediation_complexity_rival.jsonl (evidence commit 111230a) via
% output/exhibits/presentation_values.tex (macros; producer
% src/ddvc/presentation_values.py). Route-only evidence: matched
% January--June dates in 2024 and 2026, native-plus-stable denominator, one
% observation per intermediary episode, 30-day Newey--West standard errors.
% The four scope-by-complexity cells are rendered through a producer that
% refuses to emit the joint statement unless all four changes are positive.

\subsection{The software that assembles the path}
\label{sec:rivals:router}

Traders on these exchanges seldom assemble a path by hand. A router contract holds the pool set, searches it, and returns a sequence of legs, and the routers in widest use over this sample were rewritten three times: the Auto Router in September 2021, its cross-version successor that December, and the Universal Router in November 2022.\footnote{Uniswap Labs, \href{https://blog.uniswap.org/auto-router}{\emph{Introducing Auto Router}}, September 16, 2021, and \href{https://blog.uniswap.org/permit2-and-universal-router}{\emph{Introducing Permit2 and Universal Router}}, November 17, 2022. The cross-version release is dated from Uniswap Labs' \href{https://blog.uniswap.org/SuperiorReturnsForLiquidityProviders.pdf}{v2-versus-v3 liquidity-provider study} of December 16, 2021.} Each release widened the set of paths an ordinary trader could reach without extra effort. The alternative this raises is that the intermediary is a residue of the search: the software found a path through a stablecoin because such paths existed and had become cheap to look for, so the rotation would record an improvement in search and not a change in what traders were willing to hold between legs. Search does leave room to move. On parallel pools for a single leg, realised routes fall measurably short of the optimum available in the same pool state \citep{XiMoallemi2026Routing}, so a router that closes part of that gap changes which paths trades actually take.

The market-microstructure design used for staggered trading technologies is unavailable here. That literature identifies the effect of an exchange technology when the exchange grants it to some securities before others, because the securities still waiting absorb whatever else is moving in the market at the same time. A router release admits no such comparison. The contract is deployed once and is available immediately for every token pair, at every exchange the router indexes, to every trader, so there is no untreated group and no difference to take. What a symmetric window around each release can measure is whether the structure of routes moved at all when the search over them was rewritten, and that weaker object is the one the alternative needs. If automated search produced the intermediary, the incidence of intermediation should step up when the search was automated.

Route composition remains stable around the releases. Table~\ref{tab:router-windows} reports route composition over the \RouterWindowDays{} days on either side of each release, excluding the release date itself, on the daily calendar used throughout the paper. The share of economic routes carrying a third asset between the endpoints falls by \RouterIntermediationOne{} at the first release, rises by \RouterIntermediationTwo{} at the second, and falls by \RouterIntermediationThree{} at the third, moving from \RouterIntermediationBase{} before the first release to \RouterIntermediationEnd{} after the last. The largest increase at any of the three is \RouterLargestIntermediationRise{}. Path length is as still: the mean number of legs on an indirect route moves by at most \RouterLargestLegMovement{} at any release, and the share of indirect routes running past two legs changes by a few pp in either direction. Indirect routing as a whole behaves the same way, falling sharply at the first release and moving by less than half a pp at the other two.

One margin does move, and it is the margin a router should move. Among intermediated routes, the share touching more than one exchange rises by \RouterCrossOne{} at the first release from a base of \RouterCrossBase{} and by \RouterCrossTwo{} at the second; at the third it changes by \RouterCrossThree{}, from a level near a quarter of intermediated routes to \RouterCrossEnd{}. Automated search integrated the exchange set without sending trades through more assets. The distinction is the one that governs this paper's measure, which counts the asset standing between the endpoints and not the number of venues its legs visit, and it is the same separation the exchange-span stratification of Section~\ref{sec:rivals:complexity} makes within a single date.

Router releases expand exchange span while intermediary incidence remains stable. Three features of the windows bound that comparison. The first two windows overlap: the later window of the first release and the earlier window of the second share thirty days, so the first two comparisons are drawn partly from the same trading. The third window also contains a major market event. The earlier window of the third release contains the November 2022 failure of a large centralised exchange, and the composition change recorded there belongs at least as much to that event as to the router. A release date, finally, marks when a contract became callable while trader adoption can begin later, so a window straddling gradual adoption understates a movement that arrived slowly.
% output/exhibits/routing_technology_windows.jsonl via
% output/exhibits/presentation_values.tex (macros; producer
% src/ddvc/presentation_values.py) and output/tables/routing_technology_windows.tex
% (producer src/ddvc/venue_tables.py). Route-only validated evidence on the full
% daily route calendar, market-wide, with no currency split: no sentence here may
% be read as a stablecoin or native quantity. Descriptive window composition only.
% This is a descriptive route-composition comparison and does not require or
% imply a transaction-state counterfactual.
% The producer withholds every macro in this subsection if intermediation ever
% steps up by a full pp at a release, if mean path length ever moves
% by five hundredths of a leg, if intermediation stops falling at a majority of
% releases, or if the balanced five-venue perimeter stops reproducing the full
% perimeter exactly, which is what licenses treating the venue set as held fixed.

\begin{table}[tbp]
\centering
\caption{Route structure around public router releases}
\label{tab:router-windows}
\small
\input{../output/tables/routing_technology_windows.tex}
\exhibitnote{Symmetric windows of 60 calendar days either side of each release, excluding the release date. Indirect is the share of economic routes with more than one leg; Intermediated is the share carrying a third asset between the endpoints; Cross-exchange is the share of intermediated routes touching more than one exchange; Over two legs is the share of indirect routes with more than two legs; Legs and Exchanges are means per indirect route. Shares and their changes are in pp. Economic routes exclude canonical endpoint round trips. The windows are market-wide and carry no split by intermediary currency. A release is available to every trader at once, so these are descriptive route compositions.
% output/exhibits/routing_technology_windows.jsonl; producer scripts/process/build_cross_venue_routing_series.py
}
\end{table}

\subsection{The pricing rule at each leg}
\label{sec:rivals:technology}

The third alternative centers on the exchanges that price each leg, because \acp{amm} apply different pricing rules across pools. The constant-product rule sets a price from pool reserves alone and moves it with trade size relative to depth, so it charges a trade for the price impact it causes regardless of how closely the two assets substitute. Curve's invariant instead interpolates toward a flat curve in the neighbourhood of parity, which is exactly where two dollar-pegged assets trade, and Appendix~\ref{app:quoters:stable} states the interpolation the venue applies. On this account the rotation is an adoption of technology: a leg through a stablecoin became cheap once venues priced pegged assets on a curve built for them, and the vehicle role passed to stablecoins because of where their legs could be priced, so no monetary property of the asset need enter the account at all. The timing is favourable to it, since stable-specialised pools grew over the same years as the rotation.

The account can be tested by restricting attention to routes on which the pricing rule never changed. A route component enters the principal panel's constant-product scope only when every one of its legs executes at Uniswap v2 through v4 or at SushiSwap v2 or v3, each of which prices locally on the product of reserves, concentrated liquidity inside an active tick included. No leg inside that scope can have been priced by a curve specialised for pegged assets, so a technology account predicts that stablecoins gain nothing there. The quantity compared is the excess-use ratio of Section~\ref{sec:dominance:excess}, an asset's share of intermediary activity divided by its share of route-endpoint demand, formed separately inside each scope, so that the denominator for a venue family is the endpoint demand observed in that same family.

The constant-product restriction produces an even larger rotation. Where every leg is priced by the reserve product, stablecoin excess use rises from \VenueCPStableValueBase{} in 2024 to \VenueCPStableValueEnd{} in 2026 by value, a movement of \VenueCPStableValueChange{} against \VenueAllStableValueChange{} over all venues, and the ratio crosses one inside the restricted scope in the same comparison. Native assets fall to \VenueCPNativeValueEnd{} there over the same years. By count, where stablecoins were already over-represented as intermediaries at the start of the sample, the restricted and unrestricted paths are almost the same: \VenueCPStableCountBase{} to \VenueCPStableCountEnd{} inside the scope against \VenueAllStableCountBase{} to \VenueAllStableCountEnd{} over all venues. The scope also carries substantial activity, since routes priced end to end by the constant-product rule carry \VenueCPEpisodeShare{} of vehicle-currency intermediary episodes in 2026. Table~\ref{tab:venue-technology} reports every year and every venue family.

The venue that embodies the alternative supplies no intermediation at all. In each year of the sample, route components all of whose legs execute on Curve contain no intermediary episode whatever. The stable-specialised curve is used to exchange two pegged assets with one another, not as a place through which a route passes on its way somewhere else, so the pricing rule that makes stablecoin legs cheap operates on the direct trades that the vehicle measure excludes by construction. Curve legs do appear inside routes that also touch another exchange, and those routes are counted in the all-venue scope; what the sample contains no instance of is a route intermediated entirely within the specialised invariant. Balancer, whose pools mix weighted and stable families and which the panel therefore leaves technologically unlabelled, carries \VenueBalancerPeakEpisodes{} intermediary episodes in its busiest year against \VenueCPPeakEpisodes{} in the busiest year of the constant-product scope, and its ratios swing accordingly; it is reported solely to delimit the sample's composition.

What the comparison licenses is again bounded. Holding the pricing rule fixed supplies only a descriptive restriction: venues are chosen alongside routes, and a trader who would have used a stable-specialised pool may appear in the constant-product scope only because that pool was unavailable for the leg at hand. The ratio's denominator removes the most obvious composition problem, since each scope is judged against the endpoint demand observed inside it, but selection into scopes remains. The rotation therefore persists beyond stable-specialised pricing: it is present, large, and in the same direction where that pricing was never applied.
% output/exhibits/venue_technology_rival.jsonl via
% output/exhibits/presentation_values.tex (macros; producer
% src/ddvc/presentation_values.py) and output/tables/venue_technology_rival.tex
% (producer src/ddvc/venue_tables.py). Route-only validated evidence on the full
% 2,332-day calendar; complete route components; candidate-currency denominator;
% all-routes value support, which is wider than the 20% agreement support used for
% the Section 3 value results and must not be quoted as the same number. The
% Curve sentence is enforced by a producer guard that refuses every macro in this
% subsection if any all-Curve scope-year ever reports an intermediary episode.

\begin{table}[tbp]
\centering
\caption{Vehicle excess use by venue pricing family}
\label{tab:venue-technology}
\small
\input{../output/tables/venue_technology_rival.tex}
\exhibitnote{Excess use is an asset type's share of intermediary activity divided by its share of route-endpoint demand, formed over the vehicle currencies within each scope. A route component enters a scope only when every one of its legs executes at a venue in that family. The principal panel's constant-product family is Uniswap v2 through v4 and SushiSwap v2 and v3. Uniswap v1 enters the separate forced-routing architecture sample. Balancer is listed separately and left technologically unlabelled because its pools mix weighted and stable families. Value ratios use all routes; the 20\% value-agreement support of Table~\ref{tab:rotation} is narrower. Balancer has no route components before 2021, and 2026 covers January through June.
% output/exhibits/venue_technology_rival.jsonl; producer scripts/analyze/run_venue_technology_rival.py
}
\end{table}

\subsection{Liquidity in the vehicle's own pools}
\label{sec:rivals:capital}

The classical vehicle-currency mechanism links trading volume to lower transaction costs \citep{Krugman1980VehicleCurrencies}. On a decentralised exchange, the relevant markets are the pools connecting an intermediary to other assets. Trading can attract provider capital, deeper pools can lower price impact, and lower price impact can draw further trading. Pool size remains an equilibrium outcome because providers balance fee income against adverse-selection risk and the cost of adjusting their positions \citep{LeharParlour2024Uniswap}. Deposited capital measures funds committed to a pool; a size-specific quote measures how much liquidity is executable within a stated price impact. The distinction matters. Concentrated-liquidity providers can alter executable depth by changing where capital is placed along the price curve, so deposited capital and executable liquidity coincide only under particular pool designs \citep{AdamsZinsmeisterRobinson2021UniswapV3}.

That mechanism operates through the two pool venues joining an intermediary to the endpoints. Deeper stablecoin pools can lower the price impact of carrying an endpoint exchange through a stablecoin, so the classical account asks whether flow moves away from the incumbent intermediary after a stable bridge becomes feasible. Section~\ref{sec:dominance:pairs} conditions on realised routes in both endpoint years: within those matched endpoint pairs, stablecoin share changes by \MatchedMarketCountChange{} by count, with an interval that excludes increases beyond \MatchedMarketCountCIUpper{}. Bridge feasibility and adoption remain pooled in that estimate. A separate event design therefore dates the feasible-set expansion before asking how the realised route changes.
% EVIDENCE-SOURCES: output/exhibits/liquidity_capital_v2_predictability.jsonl;
% output/exhibits/liquidity_capital_v2_support.jsonl.

Let event \(e\) identify an ordered endpoint pair \((i,j)\), route scope, and the first day \(T_e\) on which DAI, USDC, or USDT has positive prior-calendar deposited capital on both route legs, with two-leg support present on at least 24 of the next 30 calendar days. The sample contains endpoint pairs that traded through WETH before \(T_e\), have no earlier observed stablecoin route, and trade on at least three days in the prior month and each follow-up window. For route counts and supported value \(q\in\{R,V\}\), the paired change is
\[
\Delta S^q_{e,h}=S^q_{e,h}-S^q_{e,[-30,-1]},
\]
where \(h\) is either the first 30 days or days 30 through 119 after bridge establishment. Share changes use the harmonic mean of pre- and post-event mass; route-intensity changes weight each event equally. Standard errors cluster by ordered endpoint pair and event date.

Panel A of Table~\ref{tab:bridge-establishment} records the change around first persistent support. Across \BridgeEstablishmentEvents{} events, stable route share rises by \BridgeEstablishmentCountCoef{} in the first month and supported-value share by \BridgeEstablishmentValueCoef{}. Native-route intensity changes by \BridgeEstablishmentNativeLogCoef{} log points, while total route intensity changes by \(-0.10\) log points. The count-share increase over days 30 through 119 is \(+8.68\) pp; the supported-value estimate is \(+6.94\) pp. These event-window estimates date stable support and the associated route allocation. The observed support event is an equilibrium outcome.
% output/exhibits/bridge_liquidity_dominance.jsonl; output/tables/bridge_establishment_regressions.tex; producer scripts/analyze/run_bridge_liquidity_dominance.py and scripts/tabulate/build_bridge_liquidity_deck_values.py.

The continuous-depth design measures the capital available on both routes after that support appears. Let \(K_{av,t-1}\) be exact prior-calendar deposited capital supporting the leg from endpoint \(a\) to vehicle \(v\), and let \(\mathcal S=\{\mathrm{DAI},\mathrm{USDC},\mathrm{USDT}\}\). Define the best stable weak-leg depth \(D^S_{et}\), WETH weak-leg depth \(D^W_{et}\), their stable relative-depth share \(Q_{et}\), and stable route-count share \(S^R_{et}\) by
\begin{equation}
\begin{gathered}
D^S_{et}=\max_{v\in\mathcal S}\min\{K_{iv,t-1},K_{vj,t-1}\},\qquad
D^W_{et}=\min\{K_{iW,t-1},K_{Wj,t-1}\},\\
Q_{et}=\frac{D^S_{et}}{D^S_{et}+D^W_{et}},\qquad S^R_{et}=\beta Q_{et}+\alpha_e+\lambda_{m(t)}+\eta_{a(t)}+\varepsilon_{et}.
\end{gathered}
\label{eq:bridge-relative-depth}
\end{equation}
Here \(W\) denotes WETH, \(m(t)\) is calendar month, and \(a(t)\) groups event age into seven-day bins. Equation~\eqref{eq:bridge-relative-depth} absorbs bridge-event fixed effects, weights by the event day's WETH-plus-stable route count, and clusters standard errors by ordered endpoint pair and date.

Panel B of Table~\ref{tab:bridge-establishment} reports the continuous-depth result. The first-month sample contains \BridgeDepthDoseFirstRows{} comparable endpoint-pair days; on \BridgeDepthThinFirstDays{} of them, the best stable weak-leg depth is below one-tenth of WETH weak-leg depth, and stablecoins carry \BridgeDepthThinFirstShare{} of route counts in that range. Within a bridge event, a 1 pp increase in \(Q_{et}\) is associated with \BridgeDepthDoseFirstCoef{} higher stable route share. Stablecoins carry \BridgeDepthEqualFirstShare{} of routes when their weak-leg depth is at least WETH depth, and \BridgeDepthDoubleFirstShare{} when it is at least twice WETH depth. Deposited depth is therefore the proximate liquidity-formation margin connecting support to route allocation. The design identifies a relationship with local capital stocks; executable quotes and causal provider-supply shifts require separate measurement and design.
% output/exhibits/bridge_liquidity_dominance.jsonl; output/exhibits/bridge_liquidity_dominance_support.jsonl; output/exhibits/bridge_liquidity_deck_values.tex

Panel C orders support and use in calendar time. Only \BridgeTimingSameDayShare{} of the \BridgeTimingEvents{} support events have a stablecoin route on the same day; \BridgeTimingMonthShare{} do within 30 days and \BridgeTimingLongShare{} within 120 days. Among the \BridgeTimingComparableEvents{} events with positive event-day WETH depth, first-month adoption is \BridgeTimingShallowMonthShare{} when stable depth is below one-tenth of WETH depth and \BridgeTimingCompetitiveMonthShare{} once it reaches that threshold. The difference is \BridgeTimingCompetitiveDiff{}, with standard error \BridgeTimingCompetitiveSE{}. Persistent support thus commonly precedes observed stablecoin routing, while relative depth separates bridges that are followed by adoption from those that remain little used. These conditional means are descriptive because route demand and provider capital are jointly determined.
% output/exhibits/bridge_liquidity_dominance.jsonl; output/tables/bridge_establishment_regressions.tex.

\begin{table}[htbp]
\caption{Stable-bridge establishment, relative depth, and route allocation}
\label{tab:bridge-establishment}
\centering
\small
\renewcommand{\arraystretch}{1.08}
\input{../output/tables/bridge_establishment_regressions.tex}
\exhibitnote{The unit is an ordered endpoint pair and route scope around the first persistent DAI, USDC, or USDT bridge. Support requires positive prior-calendar deposited capital on both route legs in Uniswap V2 or SushiSwap V2 on at least 24 of the next 30 calendar days. Every endpoint pair has prior WETH-mediated routes and no earlier observed stablecoin route. In Panel A, the pre-period is the 30 calendar days before support; share rows report paired pp changes using harmonic pre/post route or supported-value mass, and intensity rows report paired log changes with equal event weights. Panel B compares the best stablecoin weak-leg capital with WETH weak-leg capital on post-event active days. The relative-depth slope is Equation~\eqref{eq:bridge-relative-depth}; threshold rows are route-count-weighted means. Panel C records whether the first stablecoin route appears within 30 or 120 calendar days. It compares events whose exact event-day stable bottleneck is below versus at least one-tenth of WETH bottleneck depth; 12 events without a positive WETH denominator are excluded. Panel C differences give every event equal weight. Parentheses contain standard errors two-way clustered by ordered endpoint pair and date. Asterisks \(*\), \(**\), and \(***\) denote statistical significance at the 10\%, 5\%, and 1\% levels, respectively. Deposited capital measures the prior-calendar pool stock and supplies a proxy for liquidity formation; executable quotes and causal provider-supply shifts require separate measurement and design.}
\end{table}

The local bridge-depth analysis supports that earlier-margin interpretation. Let \(c=(i,j,t,s)\) denote an ordered endpoint-pair, date, and route-scope opportunity; let \(v\) index vehicles; and define \(B_{vc}=\log(1+\min\{K_{iv,t-1},K_{vj,t-1}\})\), the prior-calendar deposited capital on the weaker of the two route legs. The main route-choice specification is
\begin{equation}
\begin{aligned}
R_{vc}
 &=\beta_B B_{vc}+\theta_0\log(1+N^{(-c)}_{v,t})
   +\theta_1\log(1+N_{v,t-30})\\
 &\quad+\theta_2\log(1+P_{v,t-30})
   +\alpha_c+\delta_v+\varepsilon_{vc},
\end{aligned}
\label{eq:local-bridge-choice}
\end{equation}
where \(R_{vc}\) is the vehicle's share of five-vehicle route mass, \(N^{(-c)}_{v,t}\) is same-day leave-one-opportunity-out route reach, and \(N_{v,t-30}\) and \(P_{v,t-30}\) are prior-30-day route and endpoint-pair reach. The regression weights observations by five-vehicle route count, absorbs opportunity and vehicle effects, and clusters by ordered endpoint pair and date. For each opportunity in which at least two of the five headline vehicles have both atomic legs supported, we rank vehicles by \(B_{vc}\). The deepest supported bridge carries \BridgeLiquidityTopShare{} of five-vehicle route mass, rising from \BridgeLiquidityTopShareBase{} in 2024 to \BridgeLiquidityTopShareEnd{} in 2026, across \BridgeLiquidityGroups{} opportunity cells and \BridgeLiquidityPairs{} ordered endpoint pairs. With opportunity and asset effects, a one-log-point larger weaker-leg capital stock is associated with \BridgeLiquidityLogCoef{} higher route share, with standard error \BridgeLiquidityLogSE{}; the corresponding stablecoin total slope is \BridgeLiquidityStableLogTotalCoef{}, with standard error \BridgeLiquidityStableLogTotalSE{}. Adding the reach controls in equation~\eqref{eq:local-bridge-choice} leaves the local-depth coefficient at \BridgeLiquidityHorseRaceDepthCoef{}, with standard error \BridgeLiquidityHorseRaceDepthSE{}; same-day global route reach contributes \BridgeLiquidityHorseRaceGlobalDayCoef{}, with standard error \BridgeLiquidityHorseRaceGlobalDaySE{}. Restricting the same vehicle set to first-observed endpoint pair dates, and retaining zero-depth alternatives as part of the birth choice set, gives the entry margin directly: across \BridgeLiquidityEntryBirthRows{} vehicle rows in \BridgeLiquidityEntryBirthGroups{} entry opportunities, a one-log-point larger local bridge predicts \BridgeLiquidityEntryBirthDepthCoef{} higher route share and \BridgeLiquidityEntryBirthSelectionCoef{} higher selection probability, with standard errors \BridgeLiquidityEntryBirthDepthSE{} and \BridgeLiquidityEntryBirthSelectionSE{}. Prior-30-day route reach is estimated at \BridgeLiquidityEntryBirthLagReachCoef{} with standard error \BridgeLiquidityEntryBirthLagReachSE{}, so local depth adds information beyond existing vehicle popularity. A two-leg bottleneck specification gives the physical-market version of the mechanism: holding the stronger leg and reach fixed leaves the weaker-leg slope at \BridgeLiquidityBottleneckMinCoef{}, with standard error \BridgeLiquidityBottleneckMinSE{}, while a one-log-point leg imbalance predicts \BridgeLiquidityImbalanceCoef{} route share, with standard error \BridgeLiquidityImbalanceSE{}. The local-depth slope also survives all \BridgeLiquidityLeaveOneCount{} leave-one-vehicle reruns of the same horse race, with the weakest retained slope \BridgeLiquidityLeaveOneMinCoef{} and standard error \BridgeLiquidityLeaveOneMinSE{}, which makes the result robust to every single-vehicle omission. Inside the stable-only supported race, \BridgeLiquidityStableIssuerGroups{} opportunity cells across \BridgeLiquidityStableIssuerPairs{} ordered endpoint pairs compare DAI, USDC, and USDT directly. With the same opportunity effects, local depth, and reach controls, the 2026 USDC interaction is \BridgeLiquidityStableIssuerUsdcTwentySixCoef{} with standard error \BridgeLiquidityStableIssuerUsdcTwentySixSE{}, the USDT interaction is \BridgeLiquidityStableIssuerUsdtTwentySixCoef{} with standard error \BridgeLiquidityStableIssuerUsdtTwentySixSE{}, and local depth remains \BridgeLiquidityStableIssuerDepthCoef{} with standard error \BridgeLiquidityStableIssuerDepthSE{}. Conditional on all three stablecoin bridges being feasible in the same opportunity, the 2026 allocation is concentrated in USDC and USDT relative to DAI. Deposited capital remains a predictive state variable. Route choice is therefore organised around both vehicle identity and endpoint-pair-specific two-leg depth where endpoint-pair entry and activity growth create scope for vehicle formation.

The USDC and USDT interactions answer a different conditional question: among stablecoin bridges already supported in the same opportunity, which issuer carries more route flow in 2026? They rank feasible stablecoin alternatives after availability and do not date bridge establishment.
% EVIDENCE-STATUS: provisional exploratory local-depth and candidate-reach mechanism screen.
% output/exhibits/bridge_liquidity_dominance.jsonl and
% output/exhibits/bridge_liquidity_dominance_support.jsonl; macros from
% scripts/tabulate/build_bridge_liquidity_deck_values.py. The bridge-depth
% quantity is the weaker prior-calendar deposited-capital stock on the
% source-candidate and candidate-target route legs. Candidate reach comes from
% same-day leave-one-out and prior-30-day observed routes in the
% endpoint-candidate choice ledger. Entry rows retain zero local bridge-depth
% alternatives on first-observed pair dates. Leave-one rows rerun the same route-share
% horse race after dropping one headline candidate and retain the original
% five-candidate route-share denominator. Stable-issuer rows restrict the same
% bridge panel to DAI/USDC/USDT opportunities with at least two supported stable
% candidates and use supported stable-route mass as the denominator. Neither
% quantity is active
% concentrated-liquidity depth, executable quote depth, provider flow, or a
% causal liquidity-supply estimate. Depth rows absorb endpoint-date-scope and
% candidate effects and cluster by ordered endpoint pair and date.

A dynamic bridge analysis gives the feedback version of the volume--cost mechanism in \citet{Krugman1980VehicleCurrencies}. Let \(b\) index a source--vehicle--destination bridge, \(R_{b,t}\) be the vehicle's share of five-vehicle route mass in that local opportunity, and \(B_{b,t}\) be the log weak-leg bridge depth. The two predictive equations are
\begin{equation}
\begin{aligned}
\Delta B_{b,t+h}
  &= \beta_R R_{b,t} + \gamma_R B_{b,t} + \alpha_c+\delta_t+\varepsilon_{b,t+h},\\
\Delta R_{b,t+h}
  &= \beta_B B_{b,t} + \gamma_B R_{b,t} + \alpha_c+\delta_t+\eta_{b,t+h}.
\end{aligned}
\label{eq:bridge-feedback}
\end{equation}
Matching the same bridge at exact future horizons, a 10 pp higher current route share predicts \BridgeFeedbackRouteToDepthMonthLog{} log points more bridge-depth growth after 30 days and \BridgeFeedbackRouteToDepthLongLog{} after 120 days. In the other direction, a one-log-point larger current bridge predicts \BridgeFeedbackDepthToRouteMonthCoef{} more route-share growth after 30 days and \BridgeFeedbackDepthToRouteLongCoef{} after 120 days. The feedback margin is stronger for stable vehicles: the corresponding 120-day stable route-share to depth-growth coefficient is \BridgeFeedbackStableRouteToDepthLongLog{} for a 10 pp route-share difference, and the stable depth to route-share coefficient is \BridgeFeedbackStableDepthToRouteLongCoef{}. These rows absorb vehicle and origin-date effects and cluster by ordered endpoint pair and date. Consistent with constrained and active liquidity provision \citep{LeharParlour2024Uniswap,HuangRanaldoSchrimpfSomogyi2025Constrained}, the estimates identify predictive comovement: route use and local bridge depth forecast one another inside the same bridge. One-for-one provider funding lies outside the design.
% EVIDENCE-STATUS: provisional exploratory dynamic local-bridge feedback screen.
% output/exhibits/bridge_liquidity_feedback.jsonl; output/tables/bridge_liquidity_feedback.tex;
% macros from scripts/tabulate/build_bridge_liquidity_feedback_deck_values.py. The
% unit is the same source-candidate-target bridge observed at an exact future
% calendar horizon. Deposited-capital stock and realised route share are
% predictive state variables, not provider-flow treatments.

Table~\ref{tab:bridge-feedback} reports the coefficients from Equation~\eqref{eq:bridge-feedback}.

\begin{table}[tbp]
\centering
\caption{Dynamic feedback between route use and local bridge depth}
\label{tab:bridge-feedback}
{\scriptsize
\renewcommand{\arraystretch}{0.90}
\input{../output/tables/bridge_liquidity_feedback.tex}
}
\exhibitnote{Each cell reports a predictive coefficient with two-way clustered standard errors in parentheses. For source--vehicle--destination bridge \(b\), \(R_{b,t}\) is the vehicle share of five-vehicle route mass and \(B_{b,t}=\log(1+\min\{K_{iv,t},K_{vj,t}\})\) is deposited capital on the weaker route leg; \(\Delta\) is the exact-horizon value minus the origin-date value. Route-use rows scale \(R_{b,t}\) by 10 pp and report \(\Delta B_{b,t+h}\) in log points. Bridge-depth rows scale \(B_{b,t}\) by one log point and report \(\Delta R_{b,t+h}\) in pp. All regressions absorb vehicle and origin-date effects, control for the other current state variable, weight by five-vehicle route count, and cluster by ordered endpoint pair and date. Stable rows add the stablecoin interaction and report the stable total. Asterisks \(*\), \(**\), and \(***\) denote statistical significance at the 10\%, 5\%, and 1\% levels, respectively. These local feedback regressions use observational variation.}
\end{table}

The aggregate allocation of capital does not move in lockstep with vehicle use. In the daily five-vehicle V2 panel, WETH is the largest deposited-capital vehicle on \LiqBehWethCapitalLeaderDays{} of \LiqBehPanelDays{} days, while stablecoins lead excess use on \LiqBehStableExcessLeaderDays{} days. From 2024 to 2026, stablecoins' share of deposited capital falls from \LiqBehStableCapitalShareBase{} to \LiqBehStableCapitalShareEnd{}, while their share of intermediary route counts rises from \LiqBehStableRouteShareBase{} to \LiqBehStableRouteShareEnd{}. Their 2026 route share is \LiqBehStableRouteCapitalRatioEnd{} their capital share. Holding the date fixed and controlling for endpoint share, venue count, and pool count leaves stablecoins with \LiqBehStableControlledGapCoef{} more route share than capital share.

To examine how providers adjust after such a mismatch, let $G^S_{c,t}$ denote stable vehicle $c$'s route share minus its deposited-capital share on date $t$. For a provider outcome observed $h$ days later, we estimate
\begin{equation}
Y_{c,t+h}
= \beta G^{S}_{c,t}+X_{c,t}'\Gamma+\alpha_c+\delta_t+\varepsilon_{c,t+h},
\label{eq:lp-gap-response}
\end{equation}
where $X_{c,t}$ contains the origin-day controls named in Table~\ref{tab:lp-behavior-regressions}. The table reports the association for a 10 percentage point larger gap. The vehicle and date effects compare stable vehicles at different points in their own route-capital imbalance while absorbing common market conditions.
% output/exhibits/liquidity_provision_behavior_exploration.jsonl; output/tables/liquidity_provision_regressions.tex; producer scripts/tabulate/render_liquidity_provision_regressions.py.

\FloatBarrier
\begin{table}[tbp]
\centering
\caption{Liquidity-provision outcomes and stable route-capital gaps}
\label{tab:lp-behavior-regressions}
{\tiny
\renewcommand{\arraystretch}{0.82}
\input{../output/tables/liquidity_provision_regressions.tex}
}
\exhibitnote{Each cell reports \(\beta\) from Equation~\eqref{eq:lp-gap-response}, the association for a 10 pp larger stablecoin route-minus-capital gap \(G^S_{c,t}\), with clustered standard errors in parentheses. Capital-share change is target-date minus origin-date share within the five-vehicle V2 capital total. Because origin capital share enters both this outcome and \(G^S_{c,t}\), capital-share and capital-rank rows retain a mechanical mean-reversion component and describe convergence; identifying provider reallocation requires exogenous variation. Log-point outcomes are changes in \(\log(1+q)\), where \(q\) is deposited capital in dollars, event count, provider-day count, sender-day count, vehicle-token-side LP flow in dollars, or fee yield as named by the row. Capital-rank catch-up is origin rank minus future rank, so a positive value is an improvement; net mint-burn balance is the future mint-event count minus burn-event count. V4 accounting outcomes are same-day transaction shares in pp. Vehicle-day rows absorb vehicle and origin-date effects unless the row label implies a pool-level design: same-pool capital absorbs pool--vehicle and origin-date effects, V3 fee yield absorbs pool and origin-date effects, and V4 rows use V4-active origin vehicle-days. V3 activity-controlled and V4 activity-controlled rows include current origin-day action, origin-count, and sender-count controls; V4 flow rows include current vehicle-token-side LP-flow controls. Asterisks \(*\), \(**\), and \(***\) denote statistical significance at the 10\%, 5\%, and 1\% levels, respectively. These predictive mechanism regressions use observational variation.}
\end{table}

The V2 rows locate capital adjustment in the vehicle's pool network. A 10 pp shortfall predicts \LiqBehGapCloseLongCoef{} higher capital share and \LiqBehStableCapitalRankLongCoef{} positions of capital-rank catch-up after 120 days. The same-pool estimate is \LiqBehStableSamePoolLongCoef{}, while capital in pools already active on the origin date rises \LiqBehStablePoolEntryIncumbentLongCoef{}. Capital therefore converges mainly through reallocation across incumbent pools, with little evidence of immediate funding in the particular pool that generated the route shortfall. Origin capital share enters both the gap and the future change, so these stock coefficients include mechanical mean reversion.

The V3 rows identify activity on both sides of the provider's position. A 10 pp shortfall predicts \LiqBehStableVThreeMintMonthCoef{} more mint events and \LiqBehStableVThreeBurnMonthCoef{} more burn events over 30 days, but only \LiqBehStableVThreeNetMintMonthCoef{} in the net mint-minus-burn balance. After controlling for the vehicle's current action and provider-day counts, future provider-day activity rises \LiqBehStableVThreeOriginActivityLongCoef{} over 120 days. Fee yield changes by \LiqBehStableFeeYieldLongCoef{}. The response is therefore provider turnover and repositioning, with no corresponding increase in fee yield.

The USDC stress of March 2023 shows why route demand and provider capital must be separated. Stablecoin route share rises by \StressEventStableRouteShareChange{} inside the five-vehicle set, while stablecoin deposited-capital share moves \StressEventStableCapitalShareChange{} during the same window and \StressEventPostStableCapitalShareChange{} afterward. The episode combines peg stress, arbitrage, and demand, so it does not isolate a supply shock. It does show that an immediate route-demand increase can coincide with capital moving away from stablecoins.

V4 exposes an additional adjustment margin because the singleton records the complete swap path while settling only final net balances. A 10 pp stable route-capital shortfall is associated with \LiqBehStableVFourFlashMultilegCoef{} more multi-leg transaction share, \LiqBehStableVFourFlashInternalCoef{} more same-asset internal-leg share, and \LiqBehStableVFourFlashNettingCoef{} more gross-to-net reduction on the same day. Conditional on current vehicle-token-side flow, the shortfall predicts \LiqBehStableVFourFlowGrossLongCoef{} more future LP flow after 120 days; add and remove flow rise by \LiqBehStableVFourFlowAddLongCoef{} and \LiqBehStableVFourFlowRemoveLongCoef{}. Conditional on current activity, LP actions and sender-days rise by \LiqBehStableVFourActiveActivityLongCoef{} and \LiqBehStableVFourActiveSenderLongCoef{}. These estimates connect visible vehicle demand to provider flow and position management inside V4. They remain predictive because route demand, pool profitability, and provider expectations can move together.

Within V4, the singleton's accounting intensity permits a more direct comparison of trading paths and later provider behavior:
\begin{equation}
Y_{c,t+h}
= \beta M_{c,t} + X_{c,t}'\Gamma + \alpha_c + \delta_t + \varepsilon_{c,t+h}.
\label{eq:v4-flash-lp}
\end{equation}
In Equation~\eqref{eq:v4-flash-lp}, $M_{c,t}$ is internal same-asset routing, multi-leg transaction share, or gross-to-net settlement reduction. The controls contain origin-day swap activity, vehicle-token-side LP flow, vehicle-linked reported liquidity, LP actions, and range composition. Vehicle and date effects compare vehicle assets on the same day. Table~\ref{tab:v4-flash-lp} uses the 120-day horizon, where the earlier provider responses are most visible.
% output/exhibits/v4_flash_lp_mechanism_exploration.jsonl; output/tables/v4_flash_lp_mechanism.tex; producer scripts/tabulate/render_v4_flash_lp_mechanism.py.

\begin{table}[tbp]
\centering
\caption{V4 flash accounting and subsequent liquidity provision}
\label{tab:v4-flash-lp}
{\tiny
\renewcommand{\arraystretch}{0.88}
\input{../output/tables/v4_flash_lp_mechanism.tex}
}
\exhibitnote{Each cell reports the effect of a 10 pp increase in flash-accounting proxy \(M_{c,t}\) on the column outcome, with standard errors in parentheses. Multi-leg share is the fraction of vehicle-touching transactions with at least two swap legs; internal same-asset share is the fraction in which the vehicle asset appears on at least two legs; gross-to-net reduction is \(1-|\sum_\ell \Delta q_{c\ell}|/\sum_\ell|\Delta q_{c\ell}|\), aggregated from vehicle-token amount changes within each singleton transaction. The unit is a vehicle-day in Uniswap v4. All columns use the 120-day horizon, absorb vehicle and origin-date fixed effects, include origin-day swap activity, current vehicle-token side LP flow, current vehicle-linked TVL, current LP actions, and current range-composition controls, and cluster by origin date. LP flow is the admissible dollar value of vehicle-token sides in modify-liquidity events. Vehicle-linked TVL is full-pool provider TVL linked to a vehicle token side after address-resolution and physical TVL/volume bounds. Flow-share columns partition future dollar-valued modify-liquidity flow into narrow-or-medium versus broad ranges; action-share columns partition future modify-liquidity events into narrow-or-medium and wide-or-very-wide placements. Asterisks \(*\), \(**\), and \(***\) denote statistical significance at the 10\%, 5\%, and 1\% levels, respectively.}
\end{table}

Internal same-asset routing and multi-leg transaction share forecast more LP flow, reported liquidity, and LP actions. Dollar value shifts toward narrow or medium ranges, while the number of position updates shifts toward wider ranges. Gross-to-net reduction predicts more LP flow but little change in reported liquidity. These different signs separate two provider margins: concentrated placement of value and broader position-management activity.

Table~\ref{tab:v4-flash-gap} asks whether these margins are stronger when stablecoin route demand is high relative to deposited capital. The first two rows associate larger shortfalls with more reported liquidity and LP actions, together with a shift in position updates toward wider ranges. Gross-to-net reduction reverses those signs. The accounting proxies therefore carry different economic content even though each is enabled by singleton settlement. The corresponding flow interactions in Appendix~\ref{app:lp-detail} show less gross and add-side flow when internal or multi-leg routing is high, and more removal turnover when net settlement compression is high.
% output/exhibits/v4_flash_gap_interaction_exploration.jsonl; output/tables/v4_flash_gap_interactions.tex; producer scripts/analyze/run_v4_flash_gap_interaction_exploration.py and scripts/tabulate/render_v4_flash_gap_interactions.py.

\begin{table}[tbp]
\centering
\caption{Stable shortfalls, V4 flash accounting, and LP repositioning}
\label{tab:v4-flash-gap}
{\tiny
\renewcommand{\arraystretch}{0.88}
\input{../output/tables/v4_flash_gap_interactions.tex}
}
\exhibitnote{Each cell reports \(G^S_{c,t}\times M_{c,t}\), the interaction of a 10 pp larger stable route-minus-capital gap with a 10 pp larger V4 accounting proxy; \(G^S_{c,t}\) is stablecoin route share minus deposited-capital share and the three \(M_{c,t}\) proxies are defined in Table~\ref{tab:v4-flash-lp}. Log outcomes are in log points; range outcomes are in pp. The unit is a stable vehicle-day in Uniswap v4. All columns use the 120-day horizon, absorb vehicle and origin-date fixed effects, include origin-day swap activity, current vehicle-token side LP flow, current vehicle-linked TVL, current LP actions, and current range-composition controls, and cluster by origin date. Vehicle-linked TVL is full-pool provider TVL linked to a vehicle token side after address-resolution and physical TVL/volume bounds. Range shares partition future modify-liquidity events into narrow-or-medium, wide-or-very-wide, and full-range placements. The estimates are predictive associations from observational variation. Asterisks \(*\), \(**\), and \(***\) denote statistical significance at the 10\%, 5\%, and 1\% levels, respectively.}
\end{table}

The protocol comparison uses the same vehicle and day, which removes vehicle-date demand from the V3-versus-V4 contrast. It restricts the sample to dates with observed V4 singleton swap activity and stacks the V3 mint/burn outcome with the corresponding V4 modify-liquidity outcome. For protocol $p\in\{V3,V4\}$, vehicle $v$, date $t$, and horizon $h$, we estimate
\begin{equation}
\begin{aligned}
Y_{pvt+h}
 &=\alpha_{v,t}+\delta_p
   +\beta_h\,\mathbf 1\{p=V4\}\,\mathbf 1\{v\in\mathcal S\}\,G_{v,t}\\
 &\quad+\boldsymbol{\gamma}_h'\mathbf X_{pvt}+\varepsilon_{pvt+h},
\end{aligned}
\label{eq:v3-v4-protocol-contrast}
\end{equation}
where $G_{v,t}$ is route share minus deposited-capital share and \(\mathcal S\) is the stablecoin set. Vehicle-date effects compare the two protocols for the same vehicle and date; protocol effects absorb level differences. The coefficient \(\beta_h\) is the V4-minus-V3 difference in the association between a stablecoin shortfall and future provider behavior.
% output/exhibits/v3_v4_lp_protocol_contrast.jsonl; output/tables/v3_v4_lp_protocol_contrast.tex; producer scripts/analyze/run_v3_v4_lp_protocol_contrast.py and scripts/tabulate/render_v3_v4_lp_protocol_contrast.py.

\begin{table}[tbp]
\centering
\caption{Stablecoin shortfalls and liquidity provision in V4 relative to V3}
\label{tab:v3-v4-lp-summary}
{\scriptsize
\renewcommand{\arraystretch}{0.90}
\input{../output/tables/v3_v4_lp_summary.tex}
}
\exhibitnote{Each row reports \(\beta_h\) from Equation~\eqref{eq:v3-v4-protocol-contrast}, the V4-minus-V3 difference associated with a 10 pp larger stablecoin route-minus-capital gap, with standard errors in parentheses. The stacked unit is a protocol--vehicle--date row restricted to dates with observed V4 singleton swap activity. All specifications absorb vehicle-date and protocol fixed effects, include the corresponding protocol's origin-day provider controls, and cluster by date. V3 flows value mint and burn token sides; V4 flows value modify-liquidity token sides. Reported liquidity is full-pool TVL linked to the vehicle token and can count a vehicle--vehicle pool in both vehicle series. The estimates are conditional protocol differences and do not identify the effect of V4 adoption. Asterisks \(*\), \(**\), and \(***\) denote statistical significance at the 10\%, 5\%, and 1\% levels, respectively. Appendix~\ref{app:lp-detail} reports every outcome and horizon.}
\end{table}

Table~\ref{tab:v3-v4-lp-summary} shows that the response is strongest on margins that require active position management. A 10 pp stablecoin shortfall predicts 0.320 log points more V4-relative-to-V3 LP actions and 0.115 log points more active provider origins after 120 days. Gross and add-side vehicle-token flow differentials are small, while removal flow rises by 0.046 log points and narrow-or-medium flow share rises by 1.126 pp after 30 days. Reported liquidity grows by 0.637 log points and the pool footprint by 0.275 log points at 120 days. Thus the V4 difference is not a uniform increase in deposits. It combines more provider participation, delayed removal turnover, short-run concentration of value, and expansion across pools. This pattern is consistent with providers reallocating positions when vehicle demand exceeds committed capital, as in the adjustment-cost and inventory mechanisms of \citet{LeharParlour2024Uniswap,HuangRanaldoSchrimpfSomogyi2025Constrained}. The reported-liquidity measure assigns full-pool TVL to each vehicle token. The design identifies conditional reorganization; a causal adoption effect requires exogenous protocol timing.
% output/exhibits/v3_v4_lp_protocol_contrast.jsonl; output/exhibits/v3_v4_lp_flow_protocol_contrast.jsonl; output/exhibits/v3_v4_tvl_protocol_contrast.jsonl; output/tables/v3_v4_lp_summary.tex; producer scripts/tabulate/render_v3_v4_lp_summary.py.

% EVIDENCE-STATUS: exploratory descriptive allocation result.
% output/exhibits/liquidity_provision_behavior_exploration.jsonl and
% output/exhibits/liquidity_provision_behavior_support.jsonl; macros from
% scripts/tabulate/build_liquidity_provision_behavior_deck_values.py. Stress
% window rows use output/exhibits/stable_stress_event.jsonl and macros from
% scripts/tabulate/build_stable_stress_event_deck_values.py. The
% quantity is V2 deposited-capital stock over the five candidate vehicle
% families plus V3 mint/burn, V4 modify-liquidity event assignments, and V4
% gross/net swap-flow proxies, not provider inflow, active V3/V4 depth, unique
% LP identity, or executable quote size.
% The within-day gap row absorbs origin-date effects and controls for endpoint
% route share, venue count, and pool count. Gap-closing and rank-transition
% interaction rows absorb candidate and origin-date effects and interact the
% origin gap with stable candidate identity, with the candidate-specific rows
% replacing the stable interaction by candidate-specific gap slopes. V4 forward
% rows absorb candidate and origin-date effects plus origin-day V4 action,
% origin-count, and sender-count activity. Piecewise rows split positive route-minus-capital gaps
% from negative capital-overhang gaps under the same fixed effects. Stable-
% basket rows aggregate DAI, USDC, and USDT before joining exact future days,
% include log total route and capital controls, and use 30-day calendar HAC.
% Concentration rows join pool-candidate deposited-capital rows and summarize
% the candidate-day top-pool share, HHI, effective pool count, and pool count;
% horizon rows absorb candidate/date effects.
% Extensive-
% margin rows self-join the candidate-day panel at exact calendar horizons and
% use future log pool-count and log venue-count changes as outcomes. Same-pool
% rows join the candidate-day gap to pool-candidate deposited-capital rows and
% absorb pool-candidate and origin-date effects. Fee-incidence rows join the
% same gap to the processed V3 pool-day fee panel, then absorb pool and date
% effects; fee-yield and turnover rows normalize fees and volume by same-day TVL.
% LP-action rows join the same gaps to raw Uniswap v3 mint/burn event and
% origin-count records through the processed V3 pool metadata and absorb
% candidate/date effects; candidate-specific LP-action rows interact the same
% gap with issuer indicators. V4 comparison rows summarize raw
% modify-liquidity candidate-action assignments and map native ETH to the WETH
% candidate family; V4 activity-control rows absorb current action, origin-day,
% and sender counts; V4 flash rows compare gross and net swap-flow within
% singleton transactions. The V3/V4 flow comparison values candidate-token
% sides of provider events. These are event/provider-day counts, candidate-side
% dollar flows, and unit-free netting proxies, not whole-pool inventory, unique
% LP identity, or causal provider-supply estimates.

\subsection{Protocol architecture}
\label{sec:rivals:architecture}

Protocol design changes the set of routes that can be formed. Uniswap v1 created pools between an ERC-20 token and ETH, so token-to-token trades crossed ETH; Uniswap v2 allowed arbitrary ERC-20--ERC-20 pools and made direct token-to-token trading possible.\footnote{See Adams, Zinsmeister, and Robinson, \href{https://docs.uniswap.org/whitepaper.pdf}{\emph{Uniswap v2 Core}}, pp.~1--2.} Forced intermediation was material while the constraint lasted: token-to-token trades routed through ETH account for 8.0\% of Uniswap v1 swap transactions and 8.1\% of its ETH volume over the venue's life, and roughly one trade in ten in 2019 and 2020, when the venue was most active. A transaction from the crash weekend of March 2020 shows the mandate operating: with no direct pool between MKR and DAI, a trader selling \VOneCaseTokenIn{} MKR received \VOneCaseEth{} ETH at one exchange contract and spent the identical ETH amount at a second for \VOneCaseTokenOut{} DAI.\footnote{Transaction \ethtx{\VOneCaseTx}{\VOneCaseTxShort}, block \VOneCaseBlock, March 15, 2020. The trace is selected by a deterministic rule, the largest ETH amount routed among the \VOneCaseExactCount{} mandate-era token-to-token transactions whose two ETH legs match exactly, and the token identities are verified against the public transfer record.} Native-asset pairing remained pervasive after this mandate ended. The share of single-leg Uniswap v2 trades executed in WETH pools was 95.1\% in 2020 and 95.5\% in 2026, remaining between 93.4\% and 97.9\% in every intervening year. Among the 477,633 token pairs that ever traded on Uniswap v2, 97.1\% include WETH, and WETH's share of newly traded token pairs rose from 84.1\% in 2020 to 97.9\% in 2026. This persistence is consistent with deep WETH pools, launch conventions, and inherited search paths. Separating those forces from the original protocol constraint requires comparison among token pairs that had the same alternatives available.
% docs/findings/v1-forced-vehicle.md; output/exhibits/v1_forced_vehicle_report.md
% Registered case: output/exhibits/v1_route_case.json via output/exhibits/v1_route_case_deck_values.tex
% (macros; producer scripts/process/build_v1_route_case.py, evidence commit 38aaa86). Composition shares
% 7.97% of swaps / 8.11% of ETH volume (whole sample) and the 2019--2020 ~10% monthly shares are the
% docs/findings/v1-forced-vehicle.md section 1 composition table and monthly t2t_share series.

Uniswap v4 separates route choice from physical settlement more sharply. All pools reside in one singleton, and flash accounting records balance changes internally before settling the final net amounts at the end of a call.\footnote{See Adams et al., \href{https://app.uniswap.org/whitepaper-v4.pdf}{\emph{Uniswap v4 Core}}, Sections 1 and 3.} An economic route can therefore cross an intermediary pool without generating an external token transfer at every step. Decoded pool actions preserve the trading path despite this netting, while the architecture changes how liquidity, fees, and settlement are organised around that path.

The route evidence leaves an explanation six features to account for. The stablecoin gain occurs mainly across endpoint pairs, with little repeated substitution within matched endpoint pairs; it appears on short and long paths alike and on routes that stay within one exchange as well as routes that do not; the incidence of intermediation remained flat when the software that searches for paths was rewritten, though the exchange set it reached did widen; the gain is present where every leg was priced by the same unchanged constant-product rule, and absent from the venue whose invariant is built for pegged assets, which carries no intermediation at all; local two-leg depth predicts which vehicle carries route mass inside an opportunity; and the measured WETH-pairing persistence shows that route opportunities can outlast the protocol rule that first created them. Together they narrow the set of viable explanations. A viable explanation must instead account for why newly active and growing endpoint pairs attach themselves to stablecoins.

\section{What can account for the rotation?}
\label{sec:rivals}

The allocation results sharpen the mechanism question. Stablecoin dominance rose chiefly as routed activity moved across source--destination pairs and the incidence of intermediation changed, while repeated replacement of a native intermediary within continuing pairs contributed little. Any explanation for the rotation must therefore account for the assets being exchanged as well as the intermediary carrying the route.

\subsection{Path complexity and exchange span}
\label{sec:rivals:complexity}

Does the rotation follow from how routes are assembled rather than from which asset intermediates them? Paths grew longer and reached across more exchanges over the sample, and long paths already favoured stablecoins: in 2024 stablecoins carried \MultiLegCountBase{} of native-plus-stable intermediary episodes on routes of more than two legs, against \StableCountBase{} on two-leg routes. An account in which construction does the work therefore predicts a specific pattern. The stablecoin share should be roughly flat inside a complexity class, and the aggregate increase should come from the movement of activity toward the longer routes that were already stablecoin-intensive.

Neither part holds. Among intermediary episodes on two-leg routes, the stablecoin share rises \StableCountChange{} by count, with a standard error of \StableCountSE{}, and \StableValueChange{} by value (\StableValueSE{}). Among episodes on routes of more than two legs it rises \MultiLegCountChange{} (\MultiLegCountSE{}) and \MultiLegValueChange{} (\MultiLegValueSE{}), from a base of \MultiLegCountBase{} to \MultiLegCountEnd{} by count. Both classes move, and the class with the shortest paths moves further, so reweighting toward complex routes cannot supply the aggregate change. Dividing each complexity class again by whether the route touched one exchange or several leaves four groups, and every one of them rises; the weakest is \WeakestComplexityCountChange{} by count (\WeakestComplexityCountSE{}) and \WeakestComplexityValueChange{} by value (\WeakestComplexityValueSE{}). The stablecoin gain is thus common to short and long paths and to routes that stay on one exchange and routes that do not.

Two boundaries limit what this comparison licenses. The number of legs records the path a trade actually took, so it measures how the route was built and not how well it was chosen; a route with more legs is not thereby a worse or a better execution. Complexity and exchange span are also outcomes rather than assignments, since the same conditions that make a longer or wider path attractive can bear on the choice of intermediary. The comparison rules out a composition account of the rotation; it does not identify an effect of routing software or of the exchange set on which asset intermediates a trade.
% output/exhibits/intermediation_complexity_rival.jsonl (evidence commit 111230a) via
% output/exhibits/presentation_values.tex (macros; producer
% src/ddvc/presentation_values.py). Route-only evidence: matched
% January--June dates in 2024 and 2026, native-plus-stable denominator, one
% observation per intermediary episode, 30-day Newey--West standard errors.
% The four scope-by-complexity cells are rendered through a producer that
% refuses to emit the joint statement unless all four changes are positive.

\subsection{The software that assembles the path}
\label{sec:rivals:router}

Traders on these exchanges seldom assemble a path by hand. A router contract holds the pool set, searches it, and returns a sequence of legs, and the routers in widest use over this sample were rewritten three times: the Auto Router in September 2021, its cross-version successor that December, and the Universal Router in November 2022.\footnote{Uniswap Labs, \href{https://blog.uniswap.org/auto-router}{\emph{Introducing Auto Router}}, September 16, 2021, and \href{https://blog.uniswap.org/permit2-and-universal-router}{\emph{Introducing Permit2 and Universal Router}}, November 17, 2022. The cross-version release is dated from Uniswap Labs' \href{https://blog.uniswap.org/SuperiorReturnsForLiquidityProviders.pdf}{v2-versus-v3 liquidity-provider study} of December 16, 2021.} Each release widened the set of paths an ordinary trader could reach without extra effort. The alternative this raises is that the intermediary is a residue of the search: the software found a path through a stablecoin because such paths existed and had become cheap to look for, so the rotation would record an improvement in search and not a change in what traders were willing to hold between legs. Search does leave room to move. On parallel pools for a single asset pair, realised routes fall measurably short of the optimum available in the same pool state \citep{XiMoallemi2026Routing}, so a router that closes part of that gap changes which paths trades actually take.

Testing this as market microstructure would test a trading technology is not available here. That literature identifies the effect of an exchange technology when the exchange grants it to some securities before others, because the securities still waiting absorb whatever else is moving in the market at the same time. A router release admits no such comparison. The contract is deployed once and is available immediately for every pair, at every exchange the router indexes, to every trader, so there is no untreated group and no difference to take. What a symmetric window around each release can measure is whether the structure of routes moved at all when the search over them was rewritten, and that weaker object is the one the alternative needs. If automated search produced the intermediary, the incidence of intermediation should step up when the search was automated.

It does not. Table~\ref{tab:router-windows} reports route composition over the \RouterWindowDays{} days on either side of each release, excluding the release date itself, on the daily calendar used throughout the paper. The share of economic routes carrying a third asset between the endpoints falls by \RouterIntermediationOne{} at the first release, rises by \RouterIntermediationTwo{} at the second, and falls by \RouterIntermediationThree{} at the third, moving from \RouterIntermediationBase{} before the first release to \RouterIntermediationEnd{} after the last. The largest increase at any of the three is \RouterLargestIntermediationRise{}. Path length is as still: the mean number of legs on an indirect route moves by at most \RouterLargestLegMovement{} at any release, and the share of indirect routes running past two legs changes by a few percentage points in either direction. Indirect routing as a whole behaves the same way, falling sharply at the first release and moving by less than half a percentage point at the other two.

One margin does move, and it is the margin a router should move. Among intermediated routes, the share touching more than one exchange rises by \RouterCrossOne{} at the first release from a base of \RouterCrossBase{} and by \RouterCrossTwo{} at the second; at the third it changes by \RouterCrossThree{}, from a level near a quarter of intermediated routes to \RouterCrossEnd{}. Automated search integrated the exchange set without sending trades through more assets. The distinction is the one that governs this paper's measure, which counts the asset standing between the endpoints and not the number of venues its legs visit, and it is the same separation the exchange-span stratification of Section~\ref{sec:rivals:complexity} makes within a single date.

Three limits bound what the windows license. The dates are not independent of one another: the later window of the first release and the earlier window of the second share thirty days, so the first two comparisons are drawn partly from the same trading. Nor are they clean of the market. The earlier window of the third release contains the November 2022 failure of a large centralised exchange, and the composition change recorded there belongs at least as much to that event as to the router. A release date, finally, marks when a contract became callable and not when traders began routing through it, so a window straddling gradual adoption understates a movement that arrived slowly. What survives these limits is a narrow negative statement, and it is the one the alternative requires: at the three dates when path search was rewritten, the incidence of intermediation did not step up, while the exchange set the search reached did widen.
% output/exhibits/routing_technology_windows.jsonl via
% output/exhibits/presentation_values.tex (macros; producer
% src/ddvc/presentation_values.py) and output/tables/routing_technology_windows.tex
% (producer src/ddvc/venue_tables.py). Route-only validated evidence on the full
% daily route calendar, market-wide, with no currency split: no sentence here may
% be read as a stablecoin or native quantity. Descriptive window composition only.
% This is a descriptive route-composition comparison and does not require or
% claim a transaction-state counterfactual.
% The producer withholds every macro in this subsection if intermediation ever
% steps up by a full percentage point at a release, if mean path length ever moves
% by five hundredths of a leg, if intermediation stops falling at a majority of
% releases, or if the balanced five-venue perimeter stops reproducing the full
% perimeter exactly, which is what licenses treating the venue set as held fixed.

\begin{table}[tbp]
\centering
\caption{Route structure around public router releases}
\label{tab:router-windows}
\small
\input{../output/tables/routing_technology_windows.tex}
\exhibitnote{Symmetric windows of 60 calendar days either side of each release, excluding the release date. Indirect is the share of economic routes with more than one leg; Intermediated is the share carrying a third asset between the endpoints; Cross-exchange is the share of intermediated routes touching more than one exchange; Over two legs is the share of indirect routes with more than two legs; Legs and Exchanges are means per indirect route. Shares and their changes are in percentage points. Economic routes exclude canonical endpoint round trips. The windows are market-wide and carry no split by intermediary currency. A release is available to every trader at once, so these are descriptive compositions and not treatment effects.
% output/exhibits/routing_technology_windows.jsonl; producer scripts/process/build_cross_venue_routing_series.py
}
\end{table}

\subsection{The pricing rule at each leg}
\label{sec:rivals:technology}

The third alternative is not about currencies at all. It is about the exchanges that price them, because \acp{amm} do not price every pair by the same rule. The constant-product rule sets a price from pool reserves alone and moves it with trade size relative to depth, so it charges a trade for the price impact it causes whether or not the two assets are close substitutes. Curve's invariant instead interpolates toward a flat curve in the neighbourhood of parity, which is exactly where two dollar-pegged assets trade, and Appendix~\ref{app:quoters:stable} states the interpolation the venue applies. On this account the rotation is an adoption of technology: a leg through a stablecoin became cheap once venues priced pegged assets on a curve built for them, and the vehicle role passed to stablecoins because of where their legs could be priced, so no monetary property of the asset need enter the account at all. The timing is favourable to it, since stable-specialised pools grew over the same years as the rotation.

The account can be tested by restricting attention to routes on which the pricing rule never changed. A route component enters the constant-product scope only when every one of its legs executes at Uniswap v1 through v4 or at SushiSwap v2 or v3, each of which prices locally on the product of reserves, concentrated liquidity inside an active tick included. No leg inside that scope can have been priced by a curve specialised for pegged assets, so a technology account predicts that stablecoins gain nothing there. The quantity compared is the excess-use ratio of Section~\ref{sec:dominance:excess}, an asset's share of intermediary activity divided by its share of route-endpoint demand, formed separately inside each scope, so that the denominator for a venue family is the endpoint demand observed in that same family.

The restriction does not weaken the rotation; it sharpens it. Where every leg is priced by the reserve product, stablecoin excess use rises from \VenueCPStableValueBase{} in 2024 to \VenueCPStableValueEnd{} in 2026 by value, a movement of \VenueCPStableValueChange{} against \VenueAllStableValueChange{} over all venues, and the ratio crosses one inside the restricted scope in the same comparison. Native assets fall to \VenueCPNativeValueEnd{} there over the same years. By count, where stablecoins were already over-represented as intermediaries at the start of the sample, the restricted and unrestricted paths are almost the same: \VenueCPStableCountBase{} to \VenueCPStableCountEnd{} inside the scope against \VenueAllStableCountBase{} to \VenueAllStableCountEnd{} over all venues. The scope is not a small corner of the sample either, since routes priced end to end by the constant-product rule carry \VenueCPEpisodeShare{} of candidate-currency intermediary episodes in 2026. Table~\ref{tab:venue-technology} reports every year and every venue family.

The venue that embodies the alternative supplies no intermediation at all. In each year of the sample, route components all of whose legs execute on Curve contain no intermediary episode whatever. The stable-specialised curve is used to exchange two pegged assets with one another, not as a place through which a route passes on its way somewhere else, so the pricing rule that makes stablecoin legs cheap operates on the direct trades that the vehicle measure excludes by construction. Curve legs do appear inside routes that also touch another exchange, and those routes are counted in the all-venue scope; what the sample contains no instance of is a route intermediated entirely within the specialised invariant. Balancer, whose pools mix weighted and stable families and which the panel therefore leaves technologically unlabelled, carries \VenueBalancerPeakEpisodes{} intermediary episodes in its busiest year against \VenueCPPeakEpisodes{} in the busiest year of the constant-product scope, and its ratios swing accordingly; it is reported as a composition diagnostic and no weight rests on it.

What the comparison licenses is again bounded. Holding the pricing rule fixed by restriction is not an estimate of its effect: venues are chosen alongside routes, and a trader who would have used a stable-specialised pool may appear in the constant-product scope only because that pool was unavailable for the pair at hand. The ratio's denominator removes the most obvious composition problem, since each scope is judged against the endpoint demand observed inside it, but it cannot remove selection into scopes. The conclusion is the narrower one that the rotation is not an artifact of stable-specialised pricing, because it is present, large, and in the same direction where that pricing was never applied.
% output/exhibits/venue_technology_rival.jsonl via
% output/exhibits/presentation_values.tex (macros; producer
% src/ddvc/presentation_values.py) and output/tables/venue_technology_rival.tex
% (producer src/ddvc/venue_tables.py). Route-only validated evidence on the full
% 2,332-day calendar; complete route components; candidate-currency denominator;
% all-routes value support, which is wider than the 20% agreement support used for
% the Section 3 value results and must not be quoted as the same number. The
% Curve sentence is enforced by a producer guard that refuses every macro in this
% subsection if any all-Curve scope-year ever reports an intermediary episode.

\begin{table}[tbp]
\centering
\caption{Vehicle excess use by venue pricing family}
\label{tab:venue-technology}
\small
\input{../output/tables/venue_technology_rival.tex}
\exhibitnote{Excess use is an asset type's share of intermediary activity divided by its share of route-endpoint demand, formed over the candidate currencies within each scope. A route component enters a scope only when every one of its legs executes at a venue in that family. The constant-product family is Uniswap v1 through v4 and SushiSwap v2 and v3. Balancer is listed separately and left technologically unlabelled because its pools mix weighted and stable families. Value ratios use all routes; the 20\% value-agreement support of Table~\ref{tab:rotation} is narrower. Balancer has no route components before 2021, and 2026 covers January through June.
% output/exhibits/venue_technology_rival.jsonl; producer scripts/analyze/run_venue_technology_rival.py
}
\end{table}

\subsection{Liquidity in the vehicle's own pools}
\label{sec:rivals:capital}

The classical vehicle-currency mechanism links trading volume to lower transaction costs \citep{Krugman1980VehicleCurrencies}. On a decentralised exchange, the relevant markets are the pools connecting an intermediary to other assets. Trading can attract provider capital, deeper pools can lower price impact, and lower price impact can draw further trading. Pool size remains an equilibrium outcome because providers balance fee income against adverse-selection risk and the cost of adjusting their positions \citep{LeharParlour2024Uniswap}. Deposited capital measures funds committed to a pool; a size-specific quote measures how much liquidity is executable within a stated price impact. The distinction matters. Concentrated-liquidity providers can alter executable depth by changing where capital is placed along the price curve, so deposited capital and executable liquidity coincide only under particular pool designs \citep{AdamsZinsmeisterRobinson2021UniswapV3}.

That mechanism operates inside a market. Deeper pools joining stablecoins to other assets reduce the price impact of carrying a given source--destination trade through a stablecoin, so the classical account asks the trader to switch intermediary in a pair that was already being traded. Section~\ref{sec:dominance:pairs} measures exactly that margin, and it is the one that stays still: within pairs traded in both years, the stablecoin share changes by \MatchedMarketCountChange{} by count, and the interval around it excludes within-pair increases beyond \MatchedMarketCountCIUpper{}. Depth in stablecoin pools cannot therefore carry the rotation through continuing markets. What it can still do is act earlier, by making a stablecoin the asset to which a newly traded or fast-growing pair is connected when its markets are first formed. Separating the two roles calls for the capital committed to each candidate's pools on the same daily calendar as the routes, read in each direction on its own, because a contemporaneous association between trading and depth is equally consistent with either.
% EVIDENCE-SOURCES: output/exhibits/liquidity_capital_v2_predictability.jsonl;
% output/exhibits/liquidity_capital_v2_support.jsonl.

The local bridge-depth screen supports that earlier-margin interpretation. For each source--destination--date--scope opportunity in which at least two of the five headline candidates have both atomic legs supported, we rank candidates by the weaker prior-calendar deposited-capital leg in the source--candidate--destination bridge. The deepest supported bridge carries \BridgeLiquidityTopShare{} of five-candidate route mass, rising from \BridgeLiquidityTopShareBase{} in 2024 to \BridgeLiquidityTopShareEnd{} in 2026, across \BridgeLiquidityGroups{} opportunity cells and \BridgeLiquidityPairs{} ordered pairs. With opportunity and candidate effects, a one-log-point larger weaker-leg capital stock is associated with \BridgeLiquidityLogCoef{} higher route share, with standard error \BridgeLiquidityLogSE{}; the corresponding stable-candidate total slope is \BridgeLiquidityStableLogTotalCoef{}, with standard error \BridgeLiquidityStableLogTotalSE{}. Adding same-day leave-one-out candidate route count and prior-30-day candidate reach leaves the local-depth coefficient at \BridgeLiquidityHorseRaceDepthCoef{}, with standard error \BridgeLiquidityHorseRaceDepthSE{}; same-day global route reach contributes \BridgeLiquidityHorseRaceGlobalDayCoef{}, with standard error \BridgeLiquidityHorseRaceGlobalDaySE{}. Restricting the same candidate set to first-observed pair dates, and retaining zero-depth alternatives as part of the birth choice set, gives the entry margin directly: across \BridgeLiquidityEntryBirthRows{} candidate rows in \BridgeLiquidityEntryBirthGroups{} entry opportunities, a one-log-point larger local bridge predicts \BridgeLiquidityEntryBirthDepthCoef{} higher route share and \BridgeLiquidityEntryBirthSelectionCoef{} higher selection probability, with standard errors \BridgeLiquidityEntryBirthDepthSE{} and \BridgeLiquidityEntryBirthSelectionSE{}. Prior-30-day route reach is estimated at \BridgeLiquidityEntryBirthLagReachCoef{} with standard error \BridgeLiquidityEntryBirthLagReachSE{}, so the birth result is not simply that already popular vehicles get chosen. A two-leg bottleneck screen gives the physical-market version of the mechanism: holding the stronger leg and reach fixed leaves the weaker-leg slope at \BridgeLiquidityBottleneckMinCoef{}, with standard error \BridgeLiquidityBottleneckMinSE{}, while a one-log-point leg imbalance predicts \BridgeLiquidityImbalanceCoef{} route share, with standard error \BridgeLiquidityImbalanceSE{}. The local-depth slope also survives all \BridgeLiquidityLeaveOneCount{} leave-one-candidate reruns of the same horse race, with the weakest retained slope \BridgeLiquidityLeaveOneMinCoef{} and standard error \BridgeLiquidityLeaveOneMinSE{}, so the result is not driven mechanically by any single headline vehicle. Inside the stable-only supported race, \BridgeLiquidityStableIssuerGroups{} opportunity cells across \BridgeLiquidityStableIssuerPairs{} ordered pairs compare DAI, USDC, and USDT directly. With the same opportunity effects, local depth, and reach controls, the 2026 USDC interaction is \BridgeLiquidityStableIssuerUsdcTwentySixCoef{} with standard error \BridgeLiquidityStableIssuerUsdcTwentySixSE{}, the USDT interaction is \BridgeLiquidityStableIssuerUsdtTwentySixCoef{} with standard error \BridgeLiquidityStableIssuerUsdtTwentySixSE{}, and local depth remains \BridgeLiquidityStableIssuerDepthCoef{} with standard error \BridgeLiquidityStableIssuerDepthSE{}. The stablecoin result is therefore not only a generic dollar-asset result; by 2026 the within-dollar race is concentrated in the two operational dollar vehicles. This does not turn deposited capital into a treatment. It says route choice is organised around both issuer-specific use and corridor-specific two-leg depth exactly where market formation and corridor growth give the vehicle role room to be made.
% EVIDENCE-STATUS: provisional exploratory local-depth and candidate-reach mechanism screen.
% output/exhibits/bridge_liquidity_dominance.jsonl and
% output/exhibits/bridge_liquidity_dominance_support.jsonl; macros from
% scripts/tabulate/build_bridge_liquidity_deck_values.py. The bridge-depth
% quantity is the weaker prior-calendar deposited-capital stock on the
% source-candidate and candidate-target atomic pairs. Candidate reach comes from
% same-day leave-one-out and prior-30-day observed routes in the
% endpoint-candidate choice ledger. Entry rows retain zero local bridge-depth
% alternatives on first-observed pair dates. Leave-one rows rerun the same route-share
% horse race after dropping one headline candidate and retain the original
% five-candidate route-share denominator. Stable-issuer rows restrict the same
% bridge panel to DAI/USDC/USDT opportunities with at least two supported stable
% candidates and use supported stable-route mass as the denominator. Neither
% quantity is active
% concentrated-liquidity depth, executable quote depth, provider flow, or a
% causal liquidity-supply estimate. Depth rows absorb endpoint-date-scope and
% candidate effects and cluster by ordered pair and date.

A dynamic bridge screen gives the feedback version of the volume--cost mechanism in \citet{Krugman1980VehicleCurrencies}. Let \(b\) index a source--candidate--destination bridge, \(R_{b,t}\) be the candidate's share of five-candidate route mass in that local opportunity, and \(B_{b,t}\) be the log weak-leg bridge depth. The two predictive equations are
\begin{equation}
\begin{aligned}
\Delta B_{b,t+h}
  &= \beta_R R_{b,t} + \gamma_R B_{b,t} + \alpha_c+\delta_t+\varepsilon_{b,t+h},\\
\Delta R_{b,t+h}
  &= \beta_B B_{b,t} + \gamma_B R_{b,t} + \alpha_c+\delta_t+\eta_{b,t+h}.
\end{aligned}
\label{eq:bridge-feedback}
\end{equation}
Matching the same bridge at exact future horizons, a 10 percentage point higher current route share predicts \BridgeFeedbackRouteToDepthMonthLog{} log points more bridge-depth growth after 30 days and \BridgeFeedbackRouteToDepthLongLog{} after 120 days, with standard errors \BridgeFeedbackRouteToDepthMonthLogSE{} and \BridgeFeedbackRouteToDepthLongLogSE{}. In the other direction, a one-log-point larger current bridge predicts \BridgeFeedbackDepthToRouteMonthCoef{} more route-share growth after 30 days and \BridgeFeedbackDepthToRouteLongCoef{} after 120 days, with standard errors \BridgeFeedbackDepthToRouteMonthSE{} and \BridgeFeedbackDepthToRouteLongSE{}. The feedback margin is stronger for stable candidates: the corresponding 120-day stable route-share to depth-growth coefficient is \BridgeFeedbackStableRouteToDepthLongLog{} for a 10 percentage point route-share difference, with standard error \BridgeFeedbackStableRouteToDepthLongLogSE{}, and the stable depth to route-share coefficient is \BridgeFeedbackStableDepthToRouteLongCoef{} with standard error \BridgeFeedbackStableDepthToRouteLongSE{}. These rows absorb candidate and origin-date effects and cluster by ordered pair and date. They do not show that liquidity providers fund route demand one for one, consistent with constrained and active liquidity provision \citep{LeharParlour2024Uniswap,HuangRanaldoSchrimpfSomogyi2025Constrained}, but they move the evidence from a static sorting result toward a local feedback margin: route use and corridor depth forecast one another inside the same bridge.
% EVIDENCE-STATUS: provisional exploratory dynamic local-bridge feedback screen.
% output/exhibits/bridge_liquidity_feedback.jsonl; output/tables/bridge_liquidity_feedback.tex;
% macros from scripts/tabulate/build_bridge_liquidity_feedback_deck_values.py. The
% unit is the same source-candidate-target bridge observed at an exact future
% calendar horizon. Deposited-capital stock and realised route share are
% predictive state variables, not provider-flow treatments.

\begin{table}[tbp]
\caption{Dynamic feedback between route use and local bridge depth}
\label{tab:bridge-feedback}
\input{../output/tables/bridge_liquidity_feedback.tex}
\exhibitnote{Each cell reports a predictive coefficient with two-way clustered standard errors in parentheses. Route-use rows scale current route share by 10 percentage points and report future log weak-leg bridge-depth growth. Bridge-depth rows scale current log bridge depth by one log point and report future route-share growth in percentage points. The unit is a source--candidate--destination bridge observed at the exact future horizon. All regressions absorb candidate and origin-date effects, control for the other current state variable, weight by five-candidate route count, and cluster by ordered pair and date. Stable rows add the stable-candidate interaction and report the stable total. These are local feedback screens, not causal provider-flow estimates. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

The allocation of capital itself cuts against a flow-chasing interpretation. In the daily five-candidate V2 panel, WETH is the largest deposited-capital candidate on \LiqBehWethCapitalLeaderDays{} of \LiqBehPanelDays{} days, while stablecoins lead excess use on \LiqBehStableExcessLeaderDays{} of days. From 2024 to 2026, stablecoins' share of candidate deposited capital falls from \LiqBehStableCapitalShareBase{} to \LiqBehStableCapitalShareEnd{}, while their share of candidate intermediary route counts rises from \LiqBehStableRouteShareBase{} to \LiqBehStableRouteShareEnd{}, leaving their 2026 route share \LiqBehStableRouteCapitalRatioEnd{} their capital share. On the matched January-through-June daily calendar, the stablecoin route-minus-capital gap expands by \LiqBehStableGapChange{} with standard error \LiqBehStableGapChangeSE{}, while the WETH gap moves by \LiqBehWethGapChange{} with standard error \LiqBehWethGapChangeSE{}. Holding the date fixed, and controlling for endpoint share, venue count, and pool count, stablecoin candidates still carry \LiqBehStableControlledGapCoef{} more route share than deposited-capital share, with standard error \LiqBehStableControlledGapSE{}.

The signed gap predicts later capital reallocation, but gradually: within candidate and origin-date effects, a 10 percentage point route-minus-capital gap predicts \LiqBehGapCloseMonthCoef{} higher candidate capital share after 30 days, with standard error \LiqBehGapCloseMonthSE{}, and \LiqBehGapCloseLongCoef{} after 120 days, with standard error \LiqBehGapCloseLongSE{}. For stable candidates, the corresponding 30-day signed-gap coefficient is \LiqBehStableGapCloseMonthCoef{}, with standard error \LiqBehStableGapCloseMonthSE{}. The stable response is not homogeneous: at 120 days, the same 10 percentage point gap predicts \LiqBehDaiGapCloseLongCoef{} more DAI capital share, \LiqBehUsdcGapCloseLongCoef{} more USDC capital share, and only \LiqBehUsdtGapCloseLongCoef{} for USDT, with standard errors \LiqBehDaiGapCloseLongSE{}, \LiqBehUsdcGapCloseLongSE{}, and \LiqBehUsdtGapCloseLongSE{}. In log deposited capital the USDC and USDT responses are \LiqBehUsdcLogGapCloseLongCoef{} and \LiqBehUsdtLogGapCloseLongCoef{}. Rank transitions make the same point in a unit closer to portfolio choice: for stable candidates, a 10 percentage point route-minus-capital gap predicts \LiqBehStableCapitalRankLongCoef{} positions of capital-rank catch-up after 120 days, with standard error \LiqBehStableCapitalRankLongSE{}, while route rank moves \LiqBehStableRouteRankLongCoef{}, with standard error \LiqBehStableRouteRankLongSE{}. The response is asymmetric. In a piecewise version of the same design, a 10 percentage point stable capital overhang, meaning capital share exceeds route share by 10 points, predicts a \LiqBehStableOverhangMonthCoef{} capital-share change after 30 days and \LiqBehStableOverhangLongCoef{} after 120 days. Aggregating the three stablecoins into a dollar-vehicle basket shows where much of the adjustment comes from. With activity controls and a 30-day calendar HAC, a 10 percentage point stable-basket route-capital shortfall predicts \LiqBehStableBasketGapLongCoef{} higher stable-basket capital share after 120 days and \LiqBehWethBasketGapLongCoef{} lower WETH capital share, with standard errors \LiqBehStableBasketGapLongSE{} and \LiqBehWethBasketGapLongSE{}; the WBTC response is only \LiqBehWbtcBasketGapLongCoef{}. The extensive margin also fails the simple expansion story. For stable candidates, a 10 percentage point route-minus-capital gap predicts \LiqBehStableVenueMonthCoef{} future log venue-count change after 30 days and \LiqBehStableVenueLongCoef{} after 120 days, but \LiqBehStablePoolLongCoef{} future log pool-count change after 120 days. The stable capital that moves is also more concentrated: by 2026 its capital-weighted top-pool share is \LiqBehStableTopPoolShareEnd{}, versus \LiqBehNonstableTopPoolShareEnd{} for non-stable candidates, and the same 10 percentage point stable gap predicts \LiqBehStableHhiLongCoef{} higher future pool-capital HHI after 120 days, with standard error \LiqBehStableHhiLongSE{}.

The USDC/SVB stress window gives the same separation in event time. Stable route demand rose sharply, with stable basket route share increasing by \StressEventStableRouteShareChange{} inside the five-candidate set, while stable deposited-capital share moved \StressEventStableCapitalShareChange{} during the same window and \StressEventPostStableCapitalShareChange{} afterward. USDC's own capital share was \StressEventPostUsdcCapitalShareChange{} lower after the window, while WETH's was \StressEventPostWethCapitalShareChange{} higher. The episode combines peg stress, arbitrage, and demand, so it remains descriptive. It still falsifies a mechanical capital-chases-current-routes story: the route shock is visible immediately, while the V2 capital stock shifts away from stablecoins over the same event horizon.

A same-pool screen gives the rival its best shot. Across pool-candidate rows, a 10 percentage point route-minus-capital gap predicts \LiqBehSamePoolLongCoef{} future log deposited-capital growth after 120 days, with standard error \LiqBehSamePoolLongSE{}; for stable candidates the corresponding total is only \LiqBehStableSamePoolLongCoef{}, with standard error \LiqBehStableSamePoolLongSE{}. Splitting future exact-state V2 capital by whether the pool was already active at the origin date gives a sharper boundary: stable shortfalls predict \LiqBehStablePoolEntryIncumbentLongCoef{} incumbent-pool capital after 120 days, with standard error \LiqBehStablePoolEntryIncumbentLongSE{}, but entrant-pool capital and total capital are \LiqBehStablePoolEntryEntrantLongCoef{} and \LiqBehStablePoolEntryTotalLongCoef{}, with standard errors \LiqBehStablePoolEntryEntrantLongSE{} and \LiqBehStablePoolEntryTotalLongSE{}, and are not statistically clean in this specification. Rent incidence gives the same answer in the V3 fee archive: across \LiqBehFeeIncidenceRows{} same-pool horizon rows in \LiqBehFeeIncidencePools{} pools, a 10 percentage point stable route-minus-capital gap predicts \LiqBehStableFeeLongCoef{} future log fees and \LiqBehStableVolumeLongCoef{} future log volume after 120 days, with standard errors \LiqBehStableFeeLongSE{} and \LiqBehStableVolumeLongSE{}. Normalizing by TVL leaves no hidden rent-yield story: the corresponding estimates for future log fee yield and future log volume turnover are \LiqBehStableFeeYieldLongCoef{} and \LiqBehStableVolumeTurnoverLongCoef{}, with standard errors \LiqBehStableFeeYieldLongSE{} and \LiqBehStableVolumeTurnoverLongSE{}. Provider actions do move on a different margin. In \LiqBehVThreeLpActionRows{} candidate-days built from Uniswap v3 mint and burn logs, a 10 percentage point stable route-minus-capital gap predicts \LiqBehStableVThreeMintMonthCoef{} more future mint events and \LiqBehStableVThreeBurnMonthCoef{} more burn events over 30 days, while the net mint-minus-burn event balance rises only \LiqBehStableVThreeNetMintMonthCoef{}, with standard errors \LiqBehStableVThreeMintMonthSE{}, \LiqBehStableVThreeBurnMonthSE{}, and \LiqBehStableVThreeNetMintMonthSE{}. The provider-day version points the same way: total future mint-or-burn origin counts rise \LiqBehStableVThreeOriginMonthCoef{}, with standard error \LiqBehStableVThreeOriginMonthSE{}. Net of the candidate's own origin-day V3 action and provider-day counts, the provider-day estimate remains \LiqBehStableVThreeOriginActivityMonthCoef{} at 30 days and \LiqBehStableVThreeOriginActivityLongCoef{} at 120 days, with standard errors \LiqBehStableVThreeOriginActivityMonthSE{} and \LiqBehStableVThreeOriginActivityLongSE{}. Splitting the same design by issuer shows that the churn is not uniform: total V3 LP actions rise by \LiqBehDaiVThreeActionMonthCoef{} for DAI, \LiqBehUsdcVThreeActionMonthCoef{} for USDC, and \LiqBehUsdtVThreeActionMonthCoef{} for USDT. For USDT, the action response comes from both sides of the book, with future mint events up \LiqBehUsdtVThreeMintMonthCoef{} and burn events up \LiqBehUsdtVThreeBurnMonthCoef{}, and the provider-day response is \LiqBehUsdtVThreeOriginMonthCoef{}, yet the net event balance is negative at \LiqBehUsdtVThreeNetMintMonthCoef{}; DAI and USDC have positive net balances, \LiqBehDaiVThreeNetMintMonthCoef{} and \LiqBehUsdcVThreeNetMintMonthCoef{}. The V4 modify-liquidity archive sharpens the protocol boundary. Among \LiqBehVFourActionAssignments{} five-candidate V4 candidate-action assignments, stablecoins account for \LiqBehVFourStableActionShare{}, compared with \LiqBehVThreeStableActionShare{} in the V3 mint/burn assignment panel; USDT alone is \LiqBehVFourUsdtActionShare{} in V4, compared with \LiqBehVThreeUsdtActionShare{} in V3. V4 actions are range-management intensive: \LiqBehVFourNarrowMediumShare{} of assignments are narrow or medium tick ranges, while only \LiqBehVFourFullRangeShare{} are full-range. Those are event and provider-day counts, not dollar-valued provider flows or unique LP identities, and the V4 comparison maps native ETH to the WETH candidate family. Within candidate and origin-date effects, a one log-point higher capital stock is associated with \LiqBehLogCapitalEpisodeCoef{} higher intermediary episode share and \LiqBehLogCapitalExcessCoef{} higher excess-use ratio. These associations say capital stocks, venue reach, LP action counts, and vehicle roles are organised together, but that liquidity provision adjusts more like inventory rebalancing, range-management churn, and venue-footprint selection than immediate funding of every stable route-demand shortfall in the same pool, consistent with constrained liquidity provision in currency markets \citep{HuangRanaldoSchrimpfSomogyi2025Constrained}; they do not show provider flows causing the rotation.

The forward V4 exercise uses the singleton's own swap and modify-liquidity logs. Within candidate and origin-date effects, a 10 percentage point stable route-minus-capital gap predicts \LiqBehStableVFourFlashMultilegCoef{} higher multi-leg V4 transaction share, \LiqBehStableVFourFlashInternalCoef{} higher same-candidate internal-leg share, and \LiqBehStableVFourFlashNettingCoef{} higher gross-to-net singleton reduction, with standard errors \LiqBehStableVFourFlashMultilegSE{}, \LiqBehStableVFourFlashInternalSE{}, and \LiqBehStableVFourFlashNettingSE{}. The corresponding LP-flow specification values each candidate token side of V4 modify-liquidity events. On V4-active origin days, net of current candidate-side flow activity, the same gap predicts \LiqBehStableVFourFlowGrossLongCoef{} more future admissible LP flow after 120 days, with standard error \LiqBehStableVFourFlowGrossLongSE{}. Both sides of the book move: add flow rises \LiqBehStableVFourFlowAddLongCoef{}, with standard error \LiqBehStableVFourFlowAddLongSE{}, while remove flow rises \LiqBehStableVFourFlowRemoveLongCoef{}, with standard error \LiqBehStableVFourFlowRemoveLongSE{}. Net of the candidate's current V4 action, origin-count, and sender-count activity, the same gap predicts \LiqBehStableVFourActiveActivityLongCoef{} more future V4 modify-liquidity actions and \LiqBehStableVFourActiveSenderLongCoef{} more future sender-days. This is the liquidity-provision result connected to the V4 accounting design: the economic route remains visible even when the singleton nets settlement, and LPs respond on flow, action-count, and sender-day margins \citep{LeharParlour2024Uniswap}. It is still filtered candidate-side flow, not whole-pool TVL, a true LP inventory, or a causal provider response.

Table~\ref{tab:lp-behavior-regressions} consolidates the route-capital-gap regressions. A compact notation for the candidate-day rows is
\begin{equation}
Y_{c,t+h}
= \beta G^{S}_{c,t}+X_{c,t}'\Gamma+\alpha_c+\delta_t+\varepsilon_{c,t+h},
\label{eq:lp-gap-response}
\end{equation}
where \(G^{S}_{c,t}\) is the stable-candidate route-minus-capital gap at the origin date. The table reports the effect of a 10 percentage point larger gap. The estimate is positive for gradual capital adjustment and for incumbent-pool capital, but it is small and insignificant in same-pool capital and V3 fee yield. V3 responds through mint-and-burn churn, and V4 responds through flash-accounting intensity, candidate-side LP flow, actions, and sender-days, which is the provider-side margin closest to constrained-liquidity adjustment in \citet{HuangRanaldoSchrimpfSomogyi2025Constrained}.
% output/exhibits/liquidity_provision_behavior_exploration.jsonl; output/tables/liquidity_provision_regressions.tex; producer scripts/tabulate/render_liquidity_provision_regressions.py.

\begin{table}[tbp]
\caption{Liquidity-provision responses to stable route-capital gaps}
\label{tab:lp-behavior-regressions}
\input{../output/tables/liquidity_provision_regressions.tex}
\exhibitnote{Each cell reports the effect of a 10 percentage point higher stable-candidate route-minus-capital gap, with clustered standard errors in parentheses. Units are percentage points, log points, rank positions, or event balance as indicated. Candidate-day rows absorb candidate and origin-date effects unless the row label implies a pool-level design: same-pool capital absorbs pool-candidate and origin-date effects, V3 fee yield absorbs pool and origin-date effects, and V4 rows use V4-active origin candidate-days. V3 activity-controlled and V4 activity-controlled rows include current origin-day action, origin-count, and sender-count controls; V4 flow rows include current candidate-side LP-flow controls. These are predictive mechanism regressions, not causal provider-supply estimates. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

Table~\ref{tab:v4-flash-lp} reports the corresponding route-to-provider mechanism in the opposite direction, using the singleton's own accounting intensity at the candidate-day level:
\begin{equation}
Y_{c,t+h}
= \beta M_{c,t} + X_{c,t}'\Gamma + \alpha_c + \delta_t + \varepsilon_{c,t+h}.
\label{eq:v4-flash-lp}
\end{equation}
Here \(M_{c,t}\) is one of the three flash-accounting proxies, \(X_{c,t}\) contains origin-day swap activity, current candidate-side LP flow, current candidate-linked TVL, current LP actions, and the current narrow-or-medium range share, and the fixed effects compare candidates on the same day. The table uses the 120-day horizon because that is the interval over which the earlier V2 and V3 provider responses are most visible. Internal same-candidate settlement and multi-leg routing forecast more future LP flow, more candidate-linked TVL, and more LP actions. The position result is not tightening: those same proxies predict lower narrow-or-medium shares and higher wide-or-very-wide shares, so activity moves toward broader repositioning rather than more concentrated placement.
% output/exhibits/v4_flash_lp_mechanism_exploration.jsonl; output/tables/v4_flash_lp_mechanism.tex; producer scripts/tabulate/render_v4_flash_lp_mechanism.py.

\begin{table}[tbp]
\caption{V4 flash accounting and subsequent liquidity provision}
\label{tab:v4-flash-lp}
\input{../output/tables/v4_flash_lp_mechanism.tex}
\exhibitnote{Each cell reports the effect of a 10 percentage point increase in the row proxy on the column outcome, with standard errors in parentheses. The unit is a candidate-day in Uniswap v4. All columns use the 120-day horizon, absorb candidate and origin-date fixed effects, include origin-day swap activity, current candidate-side LP flow, current candidate-linked TVL, current LP actions, and current range-composition controls, and cluster by origin date. LP flow is the admissible dollar value of candidate-token sides in modify-liquidity events. Candidate-linked TVL is full-pool provider TVL linked to a candidate token side after address-resolution and physical TVL/volume bounds. Range shares partition future modify-liquidity events into narrow-or-medium, wide-or-very-wide, and full-range placements. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

Table~\ref{tab:v4-flash-gap} joins the two margins. The row variable is the interaction between the stable route-minus-capital gap and the same-day V4 accounting proxy, with the same candidate and date effects and the same origin-day activity controls as Table~\ref{tab:v4-flash-lp}. The estimates separate two parts of the singleton design. When a stable candidate has both a larger shortfall and more internal or multi-leg singleton routing, future candidate-linked TVL and LP actions rise, and future LP placements move away from narrow-or-medium ranges toward wide-or-very-wide ranges. Gross-to-net reduction moves the other way: it predicts lower future TVL and actions, more narrow-or-medium placement, and less wide-or-very-wide placement. The result says that the economically visible V4 route margin is connected to provider reallocation, but not because every form of net settlement compression has the same liquidity meaning. That distinction fits a view of liquidity providers as active inventory and range managers facing fee, adverse-selection, and adjustment-cost tradeoffs \citep{LeharParlour2024Uniswap,HuangRanaldoSchrimpfSomogyi2025Constrained}.
% output/exhibits/v4_flash_gap_interaction_exploration.jsonl; output/tables/v4_flash_gap_interactions.tex; producer scripts/analyze/run_v4_flash_gap_interaction_exploration.py and scripts/tabulate/render_v4_flash_gap_interactions.py.

\begin{table}[tbp]
\caption{Stable shortfalls, V4 flash accounting, and LP repositioning}
\label{tab:v4-flash-gap}
\input{../output/tables/v4_flash_gap_interactions.tex}
\exhibitnote{Each cell reports the interaction of a 10 percentage point larger stable route-minus-capital gap with a 10 percentage point larger V4 accounting proxy. Log outcomes are in log points; range outcomes are in percentage points. The unit is a stable candidate-day in Uniswap v4. All columns use the 120-day horizon, absorb candidate and origin-date fixed effects, include origin-day swap activity, current candidate-side LP flow, current candidate-linked TVL, current LP actions, and current range-composition controls, and cluster by origin date. Candidate-linked TVL is full-pool provider TVL linked to a candidate token side after address-resolution and physical TVL/volume bounds. Range shares partition future modify-liquidity events into narrow-or-medium, wide-or-very-wide, and full-range placements. The design is exploratory mechanism evidence, not causal LP-supply identification. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

The flow side shows why this is a repositioning result rather than a claim that every V4 accounting margin brings more provider-token flow. Table~\ref{tab:v4-flash-gap-flow} applies the same interaction design to future candidate-side modify-liquidity flow. Internal same-candidate routing combined with a stable shortfall predicts lower future gross flow, add flow, and remove flow, while the multi-leg interaction is negative for gross and add flow and weaker for remove flow. Gross-to-net reduction has the opposite sign, especially on remove-side flow. Thus the table that shows more future TVL, actions, and wide ranges is not hiding a simple gross-flow expansion. It says LP behavior changes through stock, action, and range-management margins, while net settlement compression is closer to turnover in the candidate-token side of modify-liquidity events.
% output/exhibits/v4_flash_gap_interaction_exploration.jsonl; output/tables/v4_flash_gap_flow_interactions.tex; producer scripts/tabulate/render_v4_flash_gap_flow_interactions.py.

\begin{table}[tbp]
\caption{Stable shortfalls, V4 flash accounting, and LP-flow sides}
\label{tab:v4-flash-gap-flow}
\input{../output/tables/v4_flash_gap_flow_interactions.tex}
\exhibitnote{Each cell reports the interaction of a 10 percentage point larger stable route-minus-capital gap with a 10 percentage point larger V4 accounting proxy. Outcomes are future log admissible candidate-side modify-liquidity flow over 120 days. The unit, fixed effects, controls, covariance, and stable-candidate sample match Table~\ref{tab:v4-flash-gap}. Add and remove flows value the candidate-token sides that can be assigned through the V4 modify-liquidity records. These are flow-side mechanism screens, not whole-pool inventory or causal LP-supply estimates. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

Table~\ref{tab:v3-v4-lp-protocol} makes the protocol comparison directly on the same candidate and day. It restricts the sample to origins with observed V4 singleton swap activity, stacks the V3 mint/burn outcome and the V4 modify-liquidity outcome, absorbs candidate-date and protocol fixed effects, and controls for the same protocol's current LP actions and active origins. A 10 percentage point stable route-minus-capital shortfall is associated with a V4-minus-V3 future LP-action differential of 0.320 log points after 120 days and a corresponding active-origin differential of 0.115 log points. The comparison does not estimate the causal effect of adopting V4. Its narrower role is to rule out a reading in which V4 merely has a higher level of counted activity: conditional on the same candidate-date origin, the stable shortfall is more strongly linked to future provider action in V4 than in V3.
% output/exhibits/v3_v4_lp_protocol_contrast.jsonl; output/tables/v3_v4_lp_protocol_contrast.tex; producer scripts/analyze/run_v3_v4_lp_protocol_contrast.py and scripts/tabulate/render_v3_v4_lp_protocol_contrast.py.

\begin{table}[tbp]
\caption{Stable shortfalls and the V4-minus-V3 LP-action response}
\label{tab:v3-v4-lp-protocol}
\input{../output/tables/v3_v4_lp_protocol_contrast.tex}
\exhibitnote{Each cell reports the V4-minus-V3 difference in the association between a 10 percentage point larger stable route-minus-capital gap and the future LP-action outcome, with standard errors in parentheses. Outcomes are log points. The stacked unit is a protocol--candidate--origin-day row, restricted to candidate-origin days with observed V4 singleton swap activity. All columns absorb candidate-date and protocol fixed effects, include same-protocol current LP-action and active-origin controls, and cluster by origin date. V3 outcomes are mint/burn event counts and active origins. V4 outcomes are modify-liquidity event counts and active origins. The design is an exploratory protocol contrast, not a causal protocol-adoption estimate. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

The dollar-flow analogue disciplines that reading. Table~\ref{tab:v3-v4-lp-flow-protocol} values the candidate-token side of V3 mint/burn events and V4 modify-liquidity events with the same daily price panel, stacks the two protocols on the same V4-active candidate dates, and repeats the candidate-date comparison for future LP flow. The V4-minus-V3 differential is near zero for gross and add-side flow at all three horizons. The one clean positive coefficient is remove-side flow after 120 days, \({0.044}\) log points for a 10 percentage point stable shortfall. Thus the protocol contrast is not a simple story in which V4 turns stable shortfalls into larger provider-token inflows. It says the same shortfalls are associated with more V4 provider activity and, on the dollar side, with delayed turnover, not broad add-flow expansion. This is why the mechanism is described as range management and reallocation, consistent with concentrated-liquidity provider adjustment costs in \citet{LeharParlour2024Uniswap}, not as a measured capital-supply shock.
% output/exhibits/v3_v4_lp_flow_protocol_contrast.jsonl; output/tables/v3_v4_lp_flow_protocol_contrast.tex; producer scripts/process/build_v3_lp_flow_candidate_daily.py, scripts/analyze/run_v3_v4_lp_flow_protocol_contrast.py, and scripts/tabulate/render_v3_v4_lp_flow_protocol_contrast.py.

\begin{table}[tbp]
\caption{Stable shortfalls and the V4-minus-V3 LP-flow response}
\label{tab:v3-v4-lp-flow-protocol}
\input{../output/tables/v3_v4_lp_flow_protocol_contrast.tex}
\exhibitnote{Each cell reports the V4-minus-V3 difference in the association between a 10 percentage point larger stable route-minus-capital gap and future candidate-token-side LP flow, with standard errors in parentheses. Outcomes are log points. The stacked unit is a protocol--candidate--origin-day row, restricted to candidate-origin days with observed V4 singleton swap activity. All columns absorb candidate-date and protocol fixed effects, include same-protocol current gross, add, remove, and sender-day flow controls, and cluster by origin date. V3 outcomes value mint and burn event token sides. V4 outcomes value modify-liquidity event token sides. The design is an exploratory protocol contrast, not a causal protocol-adoption estimate or whole-pool inventory measure. Stars denote 1\%, 5\%, and 10\% significance.}
\end{table}

% EVIDENCE-STATUS: exploratory descriptive allocation result.
% output/exhibits/liquidity_provision_behavior_exploration.jsonl and
% output/exhibits/liquidity_provision_behavior_support.jsonl; macros from
% scripts/tabulate/build_liquidity_provision_behavior_deck_values.py. Stress
% window rows use output/exhibits/stable_stress_event.jsonl and macros from
% scripts/tabulate/build_stable_stress_event_deck_values.py. The
% quantity is V2 deposited-capital stock over the five candidate vehicle
% families plus V3 mint/burn, V4 modify-liquidity event assignments, and V4
% gross/net swap-flow proxies, not provider inflow, active V3/V4 depth, unique
% LP identity, or executable quote size.
% The within-day gap row absorbs origin-date effects and controls for endpoint
% route share, venue count, and pool count. Gap-closing and rank-transition
% interaction rows absorb candidate and origin-date effects and interact the
% origin gap with stable candidate identity, with the candidate-specific rows
% replacing the stable interaction by candidate-specific gap slopes. V4 forward
% rows absorb candidate and origin-date effects plus origin-day V4 action,
% origin-count, and sender-count activity. Piecewise rows split positive route-minus-capital gaps
% from negative capital-overhang gaps under the same fixed effects. Stable-
% basket rows aggregate DAI, USDC, and USDT before joining exact future days,
% include log total route and capital controls, and use 30-day calendar HAC.
% Concentration rows join pool-candidate deposited-capital rows and summarize
% the candidate-day top-pool share, HHI, effective pool count, and pool count;
% horizon rows absorb candidate/date effects.
% Extensive-
% margin rows self-join the candidate-day panel at exact calendar horizons and
% use future log pool-count and log venue-count changes as outcomes. Same-pool
% rows join the candidate-day gap to pool-candidate deposited-capital rows and
% absorb pool-candidate and origin-date effects. Fee-incidence rows join the
% same gap to the processed V3 pool-day fee panel, then absorb pool and date
% effects; fee-yield and turnover rows normalize fees and volume by same-day TVL.
% LP-action rows join the same gaps to raw Uniswap v3 mint/burn event and
% origin-count records through the processed V3 pool metadata and absorb
% candidate/date effects; candidate-specific LP-action rows interact the same
% gap with issuer indicators. V4 comparison rows summarize raw
% modify-liquidity candidate-action assignments and map native ETH to the WETH
% candidate family; V4 activity-control rows absorb current action, origin-day,
% and sender counts; V4 flash rows compare gross and net swap-flow within
% singleton transactions. The V3/V4 flow comparison values candidate-token
% sides of provider events. These are event/provider-day counts, candidate-side
% dollar flows, and unit-free netting proxies, not whole-pool inventory, unique
% LP identity, or causal provider-supply estimates.

\subsection{Protocol architecture}

Protocol design changes the set of routes that can be formed. Uniswap v1 created pools between an ERC-20 token and ETH, so token-to-token trades crossed ETH; Uniswap v2 allowed arbitrary ERC-20--ERC-20 pools and made direct token-to-token trading possible.\footnote{See Adams, Zinsmeister, and Robinson, \href{https://docs.uniswap.org/whitepaper.pdf}{\emph{Uniswap v2 Core}}, pp.~1--2.} Forced intermediation was material while the constraint lasted: token-to-token trades routed through ETH account for 8.0\% of Uniswap v1 swap transactions and 8.1\% of its ETH volume over the venue's life, and roughly one trade in ten in 2019 and 2020, when the venue was most active. A transaction from the crash weekend of March 2020 shows the mandate operating: with no direct pool between MKR and DAI, a trader selling \VOneCaseTokenIn{} MKR received \VOneCaseEth{} ETH at one exchange contract and spent the identical ETH amount at a second for \VOneCaseTokenOut{} DAI.\footnote{Transaction \ethtx{\VOneCaseTx}{\VOneCaseTxShort}, block \VOneCaseBlock, March 15, 2020. The trace is selected by a deterministic rule, the largest ETH amount routed among the \VOneCaseExactCount{} mandate-era token-to-token transactions whose two ETH legs match exactly, and the token identities are verified against the public transfer record.} Native-asset pairing remained pervasive after this mandate ended. The share of single-leg Uniswap v2 trades executed in WETH pools was 95.1\% in 2020 and 95.5\% in 2026, remaining between 93.4\% and 97.9\% in every intervening year. Among the 477,633 pairs that ever traded on Uniswap v2, 97.1\% include WETH, and WETH's share of newly traded pairs rose from 84.1\% in 2020 to 97.9\% in 2026. This persistence is consistent with deep WETH pools, launch conventions, and inherited search paths. Separating those forces from the original protocol constraint requires comparison among pairs that had the same alternatives available.
% docs/findings/v1-forced-vehicle.md; output/exhibits/v1_forced_vehicle_report.md
% Registered case: output/exhibits/v1_route_case.json via output/exhibits/v1_route_case_deck_values.tex
% (macros; producer scripts/process/build_v1_route_case.py, evidence commit 38aaa86). Composition shares
% 7.97% of swaps / 8.11% of ETH volume (whole sample) and the 2019--2020 ~10% monthly shares are the
% docs/findings/v1-forced-vehicle.md section 1 composition table and monthly t2t_share series.

Uniswap v4 separates route choice from physical settlement more sharply. All pools reside in one singleton, and flash accounting records balance changes internally before settling the final net amounts at the end of a call.\footnote{See Adams et al., \href{https://app.uniswap.org/whitepaper-v4.pdf}{\emph{Uniswap v4 Core}}, Sections 1 and 3.} An economic route can therefore cross an intermediary pool without generating an external token transfer at every step. Decoded pool actions preserve the trading path despite this netting, while the architecture changes how liquidity, fees, and settlement are organised around that path.

The route evidence leaves an explanation six features to account for. The stablecoin gain occurs mainly across pairs rather than through repeated substitution within matched pairs; it appears on short and long paths alike and on routes that stay within one exchange as well as routes that do not; the incidence of intermediation did not step up when the software that searches for paths was rewritten, though the exchange set it reached did widen; the gain is present where every leg was priced by the same unchanged constant-product rule, and absent from the venue whose invariant is built for pegged assets, which carries no intermediation at all; local two-leg depth predicts which candidate carries route mass inside an opportunity; and the measured WETH-pairing persistence shows that route opportunities can outlast the protocol rule that first created them. Together these narrow the candidates. An explanation cannot rest on the composition of the routes that were built, on the software that selected them, on the technology that priced them, or on repeated intermediary substitution in established markets. It has to say why the markets forming and growing over this period attach themselves to a stablecoin.

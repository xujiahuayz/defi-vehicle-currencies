# Amiti, Itskhoki and Konings (2022) replication package

- Source set: `AmitiItskhokiKonings2022Dominant`
- Official package: Harvard Dataverse DOI `10.7910/DVN/ZOOWEA`, version 1.1 released 13 July 2022 under CC0. Versions 1.0 and 1.1 have identical member hashes, making version 1.1 a metadata-only update.
- Verified file: `literature/papers/2022-AmitiItskhokiKonings2022DominantReplicationPackage-supplement-dataverse-v1.1-reconstructed-20260815.tar.gz`
- SHA-256 and size: `14c3b03ed20e2363a61cecc10a7ff33801f39d1a0cefc2cf1bde36206d91c279`; 45,254,976 bytes
- Capture lineage: the 2026-08-09 capture (`...-reconstructed.tar.gz`, SHA-256 `e05fc0661ed3d1077b0c15de94b9ae35a5d57eea27dfe6bc63bc480a1d1aa09b`, 47,115,695 bytes) was assembled on another host with an unrecorded archive procedure and its bytes were never synced to this host, so the archive-level hash is not reproducible. The 2026-08-15 capture re-downloaded the same Dataverse version 1.1 in original format and re-verified every member against the publisher-declared MD5s before re-archiving deterministically; member-level identity, not archive framing, is the stable scientific identity.
- Integrity: the deterministically assembled archive contains the exact 55 official file responses in deposited original format under `DataReplication/`, their SHA-256 member manifest `MANIFEST.sha256` (manifest SHA-256 `4bc316e696ccebacd51d1ffd34625a8b2f81626d1614a753c4c827d2582285d8`) and the Dataverse version-1.1 file metadata `dataverse-files-v1.1.json`. The package directory totals 185,176,820 bytes and contains 38 Stata do-files, 12 Stata data files, two XLSX files, one XLS file, one PDF and one DOCX. Every member matches the publisher-declared MD5. Dataverse's default `.tab` responses are transformed exports; the recorded hashes and supplied code refer to the original `.dta` and workbook members.
- Reproducible assembly: Python `tarfile` in USTAR format, entries ordered as `MANIFEST.sha256`, `dataverse-files-v1.1.json`, then the 55 files sorted by path; every entry has mtime 1657670400 (2022-07-13T00:00:00Z, the version-1.1 release date), uid/gid 0, empty uname/gname, mode 0644; compressed with `gzip.GzipFile(mtime=0, compresslevel=9)`. Rebuilding from the same official responses reproduces the recorded SHA-256 exactly.
- Workflow: the package exposes the Stata specifications, table and figure mappings and includes public auxiliary data and concordances. The summary-statistics master comments out Table 1, Figures 1, 2 and A2 and Tables A1 and A2, then calls a nonexistent `TableB3.do` before later diagnostics placed after `exit`. The deposited filename is `TableB2.do`. The regression master exits before the Appendix Table B6 script. An older combined B3/B4 script remains beside the split scripts, and required empty data, temporary, log and results directories are not represented. External `gtools`, `reghdfe`, `ivreghdfe` and `estout` dependencies have no install steps or version pins, and no Stata version or environment lock is supplied. The specifications are auditable, but the masters are not runnable as deposited even with authorized data.
- Reproduction limit: the confidential National Bank of Belgium firm and customs microdata supporting the core currency-choice, pass-through and quantity estimates are not deposited and require permission and on-site access. UN Comtrade inputs for 2017 to 2019 must also be obtained separately. Public materials cannot independently reproduce the article's core firm-level estimates.
- Scientific scope: describe the article as code-auditable and data-restricted, not publicly end-to-end reproducible. The package does not turn the observational firm design into causal identification.
- Disposition: non-text companion reconstructed from exact official per-file responses, saved in the shared primary-checkout corpus and rebuildable byte-identically from the recorded assembly procedure, inspected at the version, member, code-path, data-access and reproducibility levels; it receives no paper card of its own

# Mukhin (2022) replication package

- Source set: `Mukhin2022InternationalPriceSystem`
- Official package: AEA/openICPSR DOI `10.3886/E149661V1`, version 1 published 20 January 2022 under CC BY 4.0
- Sought file: `literature/papers/2022-Mukhin2022InternationalPriceSystemReplicationPackage-supplement-openicpsr-v1-reconstructed.tar.gz`
- SHA-256 and size sought: `1e8e62e5951cf7fca535646f652b30450aac4b13f8363da8b5cd06444881ff21`; 119,236,817 bytes
- **Current disposition: unavailable (recorded 2026-08-16).** The reconstructed archive described below was inspected when this note was written, but it is no longer present. Checks run: the shared ignored corpus `literature/papers/` resolves through `ddvc.paths.literature_papers_dir` to the primary checkout and holds 109 files, none of them this archive; the three sibling checkouts (`defi-vehicle-currencies`, `-d3`, `-raw-cert`) hold only the source note; a size-filtered search across the projects tree and the glotl share finds the Amiti-Itskhoki-Konings and Somogyi packages but not this one; and `literature/papers/` is an ignored corpus that git never tracked, so no revision can restore it. Re-acquisition needs an authenticated openICPSR session and is outward-facing, so it is not attempted here. The findings gate therefore records this companion as `unavailable` rather than `materialized`.
- Integrity: the locally assembled archive contains the exact 57 official file responses and their SHA-256 member manifest. The deposited set totals 293,406,912 bytes and contains 23 Matlab files, 11 XLSX files, eight XLS files, five Stata do-files, five CSVs and one each of DTA, PDF, SHP, SHX and DBF. The member manifest hashes to `db855f9a202d82e1187c1b21d8ecdeafcd6b8c68e47c109fe848a22195c31d48`.
- Workflow: the package maps scripts to tables and figures and discloses public input-output, exchange-rate, inflation, trade, invoicing, markup and concordance inputs. `Master.m`, `Model.m`, `Fit.m` and `Stata/Master.do` are present and inspectable.
- Reproduction limit: `Master.m` calls `Fit_Swiss` unconditionally although the required confidential Swiss customs file is not deposited, which stops the advertised master run before later counterfactual and robustness sections unless the call is bypassed or authorized data are supplied. The Stata markup stage uses an author-specific absolute path and requires manual transfer into `ICIO_sectors.xlsx`; output directories are manual; Mapping Toolbox use is undeclared; and the package has no environment lock, saved benchmarks or numerical assertions. Active code is deterministic, but model variants use a tolerance-based fixed-point loop without an iteration cap, explicit matrix inverses and `real()` to discard numerical complex residue.
- Scientific scope: the materials support auditing calibration, model fit, scenario construction and most public-data outputs. Counterfactuals remain conditional calibrated scenarios, not causal estimates, transition probabilities or uncertainty bands.
- Disposition: non-text companion recorded as unavailable. It was reconstructed from exact official file responses and inspected at the inventory, code-path, dependency, numerical, data-access and claim levels, and the findings above are the durable record of that inspection; the archive itself is no longer held (see the current disposition above). It receives no paper card of its own, and no claim may cite the package's contents or any reproduction run from it. The card `Mukhin2022InternationalPriceSystem` keeps `Status: claim-verified`, which rests on the published article and the official 37-page online appendix, both saved and read in full.

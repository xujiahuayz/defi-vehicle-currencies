# Empirical runtime data

Large panels and resumable caches that predate the `data/processed/` boundary live
here. Move a panel to `processed/` when its current owner is rebuilt. See
[`README.md`](../../README.md).

# Legacy metadata

This directory exists temporarily for compatibility with older writers. Do not add
new content hashes, fingerprints, provenance sidecars or release certificates.
Retire existing entries together with the code path that still reads them. The
current workflow uses declared paths and direct rebuilds; see
[`README.md`](../../README.md).

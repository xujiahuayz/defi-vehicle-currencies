#!/usr/bin/env python3
"""Fail closed on the withdrawn fixed-clock dominance-window diagnostic.

The retired implementation mixed a fixed-notional route grid with a pooled extra-hop gas
constant, one panel-wide gas price and fixed ETH/USD. Its gross and all-in exhibit is not a
registered finding. A replacement must declare whether it is a gross output diagnostic or
an all-in cost object; an all-in replacement must use the registered gas-price and
receipt-calibrated route-unit owners at the estimand's clock.
"""

from __future__ import annotations


def main() -> int:
    raise RuntimeError(
        "dominance-window diagnostic withdrawn pending a registered gross-only or exact-clock all-in replacement"
    )


if __name__ == "__main__":
    main()
